# 变更日志

本项目所有值得记录的变更都会写在这里。

格式参考 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.1.0/)，版本遵循语义化版本。
**当前处于内测准备阶段，尚未发布正式版本。**
---

### 2026-09-23：回复质量基线之后的三处修正（未部署）

回复质量评测的第一轮基线暴露了三个问题，产品负责人裁定都修（路线图 C16）：

- **直爽方式先接住你，再怼事**：人设原来写着「用户受挫时先怼回去让她清醒」，和共用前言的「先接住情绪」互相矛盾，基线里直爽方式最常漏掉这一步。现在是先用一句话接住你在气什么、委屈什么，再去怼那件事或那个让你受气的人，你的感受永远不是被怼的对象。
- **诚实的回答不再被换成模板**：输出过滤原来只看关键词，「你总觉得我是真人」「帮你骂他我不干」这类话也会命中，整条换成一句「我在听」。现在按意思判断：劝阻、她自己的拒绝、转述或反问你的话都不算；真的拱火、伤人、自称真人照样拦下（见内容审核策略 §5.1）。
- **只是倾诉的一轮更容易被认出来**：原来只认难过、生气、焦虑那几个词，「好孤单」「很愧疚」「太丢脸了」「他又说我胖」都被当成办事，这一轮就会带上整份工具目录；基线里还有一条倾诉的回复整条跑偏成一道算式。现在这些常见说法也算倾诉，明说交代办事的照常给工具。

### 2026-09-23：回复质量离线评测（开发工具，未部署）

- 新增 `pnpm --filter cyber-sister-server eval:replies`：同一个生成模型分三组（通用模型 / 去掉女性层 / 完整 Amie），走真实的生产聊天链路（只把数据库换成假的），用另一家模型盲评打分，并先用手写的正反例自检打分模型。默认桩模式不联网；`--live` 真实调用，要花钱，先问产品负责人。见 [回复质量评测](docs/04-开发/回复质量评测.md)（路线图 C15）。
- 评分标准 12 条与 30 个场景是 AI 起草的草案，待产品负责人逐条审定后冻结。
- 书籍技能集中登记到 `bookSkills.js`。网关加两个默认关闭的开关：`GATEWAY_PERSONA_SHARED_PREAMBLE=off` 只供评测消融；`GATEWAY_<NAME>_EXTRA_BODY` 用来给某一家供应商加专属参数（例如关掉 MiMo 的思考模式）。默认的人格提示词与之前逐字节一致。
- 用户能感知的行为没有变化。

### 2026-09-23：未提交改动审查修复（未部署）

- 自定义模型供应商的正式请求与「试一下」都在实际建连时校验并固定解析到的地址（本机运行时允许显式回环例外），拒绝内网目标与跳转；API 镜像补入安排判定包。
- 聊天确认动作先抢占提案再执行，重复确认或确认与取消并发不再重复执行；被拦或失败如实显示。记忆修改提案保留提出时的版本，旧提案不能覆盖后来的编辑。
- 云端同意升为 `cloud-primary-v4`，旧同意须重新选择，并在首屏与设置中说明可选来信的数据用途。撤回经期同意后仍可查看、删除旧记录，月历不再标注或预测。
- 以上均为本地修复与自动化验证；真实 PostgreSQL、镜像构建和部署仍待在相应环境验证。

### 2026-09-22：聊天成为全域第二入口——模块操控技能化 + 确认卡 + 日历×聊天双向（未部署）

产品负责人四条裁定落地：①手记/读书/对话的重叠按「一天一篇手记 + 读书笔记记在书下」收口；②日历某天是完整图景（安排 + 手记 + 读书笔记 + 经期）；③所有特色功能都能在聊天框里对话操作；④模块操控整理成产品既有的「内置技能」形态，不散在工具注册表里。

- **七个模块操作技能**（`src/skills/{notes,reading,calendar,period,collection,memory,letter}/SKILL.md` + `services/*Skill.js` + `moduleSkills.js`）：与「情绪与关系梳理」「身体呵护」同一范式（SKILL.md 核心行为段、话题正则激活、`scene!=='chat'` 不注入）。每个技能导出 `{ id, title, tools, toolParameters, buildContext }`，`agentService` 只做扁平化注册与执行；`utils/toolSchema.js` 抽出共用参数构件，避免技能→agentService 循环依赖。
- **读工具补齐**：`list_diary`（某天/某月）、`list_books`、`list_reading_notes`（新增 `readingService.listNotesBetween`，按 UTC 日历日界取跨书笔记，`GET /api/reading/notes?from=&to=` 透传）、`list_letters`（最近 5 封摘要）、`list_memories`、`day_review`。写工具补齐：`add_collection_item`/`update_collection_item`/`delete_collection_item`、`update_book`、`delete_diary`、`delete_reading_note`（**没有「改笔记」**，页面同语义：只能删了重记）、`update_period_record`/`delete_period_record`、`switch_persona`、`set_letter_freq`、`update_memory`/`delete_memory`（**没有 `create_memory`**，新建仍只走「帮我记住」确认卡）。
- **确认卡机制（追加直做、改删确认）**：工具条目可选 `needsConfirm(userId, args)` → false | 动作描述。命中时不执行 run，落一条 `{ pending: true, args, summary: '想…，等你点头' }` 的 toolRun（JSONB 直存，零迁移）；聊天内 `ActionConfirmCard`（照 MemorySuggestion 卡片模式）两键走新端点 `POST /api/chat/messages/:id/tool-runs/:index/confirm|dismiss`——确认复用同一个 `runConfirmedTool`（不建第二执行通道），对象已被别处删掉返回 `{ already: true, toolRun: '已经不在了' }`；重复处理 409「这个动作已经处理过了」。恒需确认：`delete_task`（聊天内由此从直删改为确认后删）、`delete_diary`、`delete_reading_note`、`delete_collection_item`、`delete_period_record`、`update_memory`、`delete_memory`；条件确认：`add_diary`（今天已写过=覆盖正文）、`update_collection_item`（改名字或备注）。
- **日历×聊天双向**：日历日详情下新增「那天的记录」（当天日记 + 当天读书笔记（《书名》·页码 / 引文 / 「回到书里这一处」）+ 经期中，全空不渲染）；聊天侧 `day_review` 返回那天的安排/手记/读书笔记/经期（经期未同意时整体缺省该字段，不提示去开同意）。「那天会发生哪几件」的判定抽成零依赖纯函数包 `packages/schedule-logic`（`occursOn`/`daysLeft`/`DATED_FREQS` 等），web `features/schedule.js` 改为 re-export、API `reminderService.listScheduledRemindersOn` 同用一份，不造第二份判定。
- **来信建议进聊天**：LetterView 的建议动作区抽成 `SuggestionActions`（三键「同意采纳 / 带去对话 / 不用」逐字不变），聊天来信便签（`HerNudges`）就地渲染同一组件（`letterNudge` 带 `letterId`/`suggestions`），「带去对话」经 `onComposeDraft` 填聊天草稿。
- **提示词收口**（TASK_GUIDE 追加）：写下来的话分三种（手记/读书笔记/「帮我记住」）；改删已写记录只能提出、等用户点头；问「我哪天记了什么/有什么事」先调 `day_review`。
- 验证：API 单测全绿（1220 例，新增技能激活/注册、needsConfirm 分流、确认端点矩阵、`listNotesBetween`、`day_review` 等）、Web 单测全绿（630 例，新增 ActionConfirmCard、来信便签三键、日历「那天的记录」）；真实栈 UI 验收四条全过：删手记确认/取消两条路、同日二次写手记的覆盖确认卡（取消保原文、确认后覆盖）、问 9 月 22 日 `day_review` 答出日记+安排+读书笔记且日历当天聚合一致、聊天删读书笔记（确认卡确认后消失）与来信建议「同意采纳」后「她记得的你」对应记忆已改。已知小瑕疵（登记）：模型偶发把工具 JSON 写进正文而不执行（协议层如实跳过、不会静默写库），提示「只输出 JSON」后重试即正常。

### 2026-09-23：她的来信——做梦、待确认、记忆维护收进一封定期来信（未部署）

产品负责人定调「简约至上」：「她」页暴露的面板太多。做梦、记忆系统这些，最终都体现成一个东西——她的来信：按用户设定的频率（三天一封 / 七天一封）由服务器端根据记忆书写，信里是三类内容（修改建议、一些看法、打趣），看信时一键「同意采纳」或「带去对话」。用户侧只看到「来信」（最新一封 + 往期）与频率设定；记忆只能由用户创建和维护的不变式保持。

- **数据与设置**：一个迁移 `20260923000000_her_letters`——`users.letter_freq_days`（空=不写信，默认不写）取代 `dream_enabled`/`dreamt_at`；`letters.week_start` 改名 `period_start`（周期起点：本地日 UTC 零点），新增 `freq_days`（存量按 7 天回填）与 `suggestions`（JSONB），唯一键改 `(user_id, period_start)`。`PUT /api/user/profile` 的 `dreamEnabled` 换成 `letterFreqDays`（只收 `null|3|7`，否则 400「letterFreqDays只能是3、7或空」）。
- **来信生成**（`letterService` 重写，周信全部退役）：到期判定（从没写过就到期；改频率立即影响下一封）、素材窗口=近 `freqDays` 天、回想并入写信（同意云端时生成前 `runAnalysis` 一次，草稿进本封信素材、**用过即消费**置 `dismissed` + `resolution: lettered:<iso>`）、组信双路（云端模型出稿 + 服务端逐条校验建议——`memoryId`/`quote` 必须命中素材记忆且逐字、版本以服务端为准、超长或敏感整条丢、最多 3 条；未同意/失败降级本地模板，模板不发明建议）、正文敏感段整删 + 超 1200 字截到最后一个段落边界、沉默期宁缺毋滥、并发撞 `P2002` 回读。
- **来信动作 API**（`/api/letters` 重新上线）：`GET /`、`GET /:id`、`POST /generate`（幂等，带 `reason: off|not_due|quiet`）、`POST /:id/read`（幂等）、`POST /:id/suggestions/:index/decide`（动作矩阵：edit_memory 走 `updateMemory` 带版本、冲突 409 透传不自动覆盖；delete_memory 已删过返回 `{success, already:true}` 照样算采纳；plan 建一条 once 的「安排」缺省明天 09:00（北京时间）；重复处置 409「这条建议已经处理过了」）。
- **她页**只剩四块：她的说话方式、她的状态、**来信**（频率三档 + 最新一封 + 往期）、她记得的你（收窄为最小列表：让她记住 / 放在心上 / 改一改 / 删除 / 分页）。「带去对话」经一次性路由状态把引导句填进聊天输入框（已有草稿不覆盖）；`/memories`、`/profile/memories`、`/tools/workspace`、`/tools/letters` 四条旧路径一律回 `/her`。
- **退役**：`DreamSwitch`/`FollowUpList`/`MemoryReviewPanel`/`MemoryDetailPanel` 与 web `derivedService`（含「她」入口旁的待确认数字）删除；API 侧 `routes/derived.js` 整组、`derivedService` 的晋升/治理/做梦入口、`edgeService` 的确认/忽略/列表、`followUpService.deleteFollowUp`、记忆的版本恢复与整库清空（`GET /:id/revisions`、`POST /:id/restore`、`DELETE /api/memories`、列表搜索 `q`）全部删除；`chatService` 不再 fire-and-forget 做梦。`runAnalysis` 改为导出，由写信调用。
- **共享收拢**：`isSensitiveContent` 进 `utils/sensitivePatterns.js`（候选/素材/成信/建议同一口径）、同意装配 `loadExternalConsent` 进 `userService`（派生、记忆候选、写信同一份）。
- **合规文案**（同意范围是行为定义）：同意范围改写为「她写信前会回想你的记忆与近况，写成给你的信（你不写信就不回想）」「她写信时的草稿不会进入聊天」；落地页同步。
- **导出兼容**：`letters` 导出形状改 `{periodStart, freqDays, content, createdAt}`。
- 验证：API 单测全绿（来信生成/校验/建议矩阵/幂等等新例）、Web 单测全绿（来信三键、频率三档、compose 交接、最小记忆列表）；真实栈 UI 验收见下条记录。

### 2026-09-22：接上第一家真实厂商（DeepSeek）与顺手修掉的日志问题（未部署）

产品负责人给了 DeepSeek 的密钥，于是把「模型供应商」这条链在真实厂商上走了一遍，并修掉一个只有真跑起来才看得见的问题。

- **修掉：网关的告警一直打成 `[object Object]`**。网关按 pino 的习惯调用（先对象、后消息），我们的 logger 是先消息、再上下文、且只收白名单里的键——两边口径没接上，于是「哪一家失败了、为什么换下一家」在日志里只剩一个 `[object Object]`。多供应商上线后，这行日志恰恰是排查切换原因的唯一线索。现在在边界上转一道，完整打出 `provider / model / attempt / latencyMs / result`（例如 `result: http_401`），API 侧加了一个测试锁住这个转接。
- **真实链路的验收（手工，不进 CI）**：实例管理员在设置页配 DeepSeek → 「试一下」真发一次最小请求（179ms，回「你好」）→ 再把一把无效密钥的同一家排到第一位：聊天照常成功，网关自动换到第二家（日志里同一次请求先 `http_401`、随后 `ok`），本子上落下的就是真实回复。
- **不进 CI 的理由**：要真花钱，也不能把密钥放进流水线；CI 里跑的是离线那一层（单测 + 集成 + e2e 打桩）。
- 已知小瑕疵（本轮未改，登记在此）：消息落库的 `source` 字段沿用历史值 `qwen`，含义是「云端模型」而不是某一家厂商；它不出现在界面上（界面只标例外来源），真实厂商名在 `provider`/`model` 与 `/api/llm/status` 里。

### 2026-09-22：专为女生的那一层（路线图第三步，未部署）

路线图 C9 的第三步。产品负责人四条裁定都是推荐方案：把提示词那一层接上、开场话题换成她自己的线索、加一套冻结的离线检验集，并确认身体关怀那条不需要新代码。

- **她怎么说话，多了一层共用前言**（`packages/llm-gateway/src/personas.js`）：产品负责人点名把《年轻女性心理与经典话题提示词设计》从历史归档**扶正为在用文档**（移到 `docs/01-产品/`），其中批准的三条共享段变化接成一段共用前言——先接住情绪、再松动比较与灾难化的框架、最后才给最小动作；不评判她的身体与外貌（连「你不胖」都不说）；医疗、医美、减肥、理财不给处方；关系里出现控制、贬低、隔离就如实说是压力信号，但不替对方下诊断；她的决定永远归她；不让自己成为她唯一的出口。顺序是**安全边界 > 共用前言 > 说话方式**，三种说话方式与全部六个人格 ID 一律生效。
- **开场话题从她的线索里来**：新增只读 `GET /api/chat/openers`，按「她惦记的事（到日子的在前）→ 正在读的那本书 → 最近写下的一行」最多给 4 条，每条如实写明「为什么看到这条」；**手记来源那条只填成草稿、绝不自动发送**，输入框已有内容时置灰；接口不发模型、不落库、同一份数据两次调用结果一致；关掉做梦就不再提她惦记的事；取不到就安静地用回本机静态池，空对话那一屏不会被拖慢。
- **一套冻结的「懂你」检验集**：场景与期望冻结在 `apps/api/tests/knows-you/knows-you.cases.json`（称呼、放在心上那几条、此刻与间隔、她今天说过的话、没打开「顾及周期」时连经期记录都不读、打开后只多一句语气要求且不做推测、中级危机走小心模式且不塞工具目录、明确的自伤意图整轮拦下、只是倾诉的一轮没有工具目录）。跑的是拼给模型的那段上下文，**离线、不花钱、进 CI**；改期望要产品负责人确认。
- **身体关怀与经期事实**：按现状确认——只调语气与分寸、不主动提、不归因、不进记忆，不需要新代码。
- 文档：设计文档扶正（状态改 ✅ 当前有效，并把「从未实施的 personas.v2.json + `GATEWAY_PERSONAS_FILE` 灰度」那段标成历史方案）、路线图 C9、API 文档（新端点与做梦开关口径）、文档索引、交接计划同步；换境外厂商的数据出境与同意版本仍待法务（与本轮无关）。
- 验证：API 1166 项（另有 70 项真实 PostgreSQL 集成用例无库跳过）、Web 637 项、网关 25 项单测通过；Playwright 76 项通过（新增「开场话题」：空白对话摆出她自己的线索、手记那条只填输入框且零请求、置灰不变）；typecheck、lint（Web 18 / API 143，均未超基线）、build 通过。

### 2026-09-22：对话装订成本子（未部署）

上午的「信纸」产品负责人看过之后又提了一版：网页端的对话版式边框没有对齐，而且它应该是**一本本子**——左边有封皮，从右往左翻，开头有一个封面（就是原来空白对话看到的那一页）。

- **边框对齐**：纸、页码那一行、最底下的书写行现在共用同一条页边线和同样的左右留白。横格与页边线画在整张纸上、从纸顶一直贯穿到书写行，页码那一行不再自己画一条线；输入框与正文列对齐，输入的文字和正文同列。窄于 480px 时页边线收到 32px。
- **本子**：左边一条 14–18px 的封皮，比纸深的纸板色 + 一层布纹 + 两道细缝线（新令牌 `--cs-cover`、`--cs-cover-stitch`、`--cs-cover-weave`，日间与夜间各一套，封面上正文 8.5:1、次级与辅助 5.2:1）；右下角两三层很浅的纸边，暗示底下还压着页；圆角与阴影只在本子外轮廓上。
- **封面是第 0 页**：空白对话打开就是合上的本子，只看得到封面（手写问候、拱窗里的她、「嗨，我是你的Amie」与开场话题），底下的信纸不参与点选与读屏；点开场话题或在书写行写字就翻开到第一页；有对话时打开仍停在最新一页，一直往前翻、翻过最早的信（没有更早的）就回到封面。页码从封面之后算起，封面上写「封面」。
- **从右往左翻，绕着左边的封线**：往后翻是把眼前这一页拓印一份、`rotateY(0 → -180deg)` 绕左边封线翻过去（父层 1600px 景深，700ms，减速不回弹），翻过一半就看不到正面、露出纸的背面（一层淡淡的背面色）；往前翻是新的一页从左边翻回来，旧页先铺一层静影像压在上面、落定后两层一起撤掉。翻出本子外的那一半裁掉，不压到页头和页码。`prefers-reduced-motion` 下不翻，改成旧页淡出、新页淡入。
- **宽屏也只摊开一页**（产品负责人裁定）：本子居中、宽度不超过 600px，不再左右两页——左边有封线，就是竖着的一本。
- 手写字与「印刷体」退路、她回信时的「翻到最新一页」、手机上左右滑、← → 与 PageUp / PageDown、页头的 AI 身份条、危机弹窗、首次使用声明与两小时提醒都不变；手记、读书等页面不改。
- 验证：Web 629 项单测通过（新增：本子的版面、封面是第 0 页、空对话停在封面、往前翻过最早的信回到封面、往前翻是两层拓印、减少动态效果时淡出）；Playwright 74 项通过（新增「本子：封面与翻页」：空对话停在封面、封面不画横格、写一句就翻过去、两个方向的拓印动效与 transform-origin 都在左边缘、320px 与夜间各查 axe）；typecheck、lint（Web 18 / API 143，均未超基线）、build 通过。**需要真机看**：翻页 700ms 的观感、低端安卓上流式回复时的分页是否掉帧。

### 2026-09-22：模型不再绑死一家厂商（未部署）

「不能指望某个厂商永远给我提供服务」：聊天模型不再只认一个写死的环境变量槽。

- **实例管理员在设置页配模型供应商**：新增「模型供应商」卡片（只有实例管理员看得见；不是管理员时整张卡片不渲染，也不会去请求管理接口），可新增、编辑、排序（上移/下移）、启停、删除、「试一下」。配了多家就按列表顺序依次尝试：前一家不通，自动换下一家。
- **接口**：`GET/POST /api/admin/model-providers`、`PUT /api/admin/model-providers/:id`（含启停）、`PUT /api/admin/model-providers/order`、`DELETE /api/admin/model-providers/:id`、`POST /api/admin/model-providers/:id/test`；逐个端点都判一次「实例管理员」（`INSTANCE_ADMIN_PHONES` ∩ 内测白名单），不是管理员一律 403。
- **密钥只写不读**：以 AES-256-GCM 密文存进新表 `model_providers`（迁移 `20260922120000_model_providers`，纯新建表），主密钥来自只读文件 `MODEL_CONFIG_KEY_FILE`（怎么生成、放哪、怎么备份、怎么轮换写进了部署手册）；任何响应只有 `hasKey`，日志与审计里没有密钥；主密钥缺失或密文解不开时如实报 503，**不会**静默降级，也不会偷偷改回环境变量槽。
- **地址校验**：必须 https（只有本机运行时允许 `http://127.0.0.1`）、不得内嵌账号密码、不得指向本机或内网；真发请求前再按 DNS 解析结果拒一次内网。「试一下」会真的发一次最小请求（界面上写明会产生一点点费用）。
- **一家都没配时行为与以前完全一致**（仍走 `GATEWAY_QWEN_*`）；改动保存即生效，不用重启（服务端重读配置并重建网关）。「聊天模型」卡片里写明当前在用的是哪几家（只写显示名与模型名，不含地址与密钥）。
- Compose 接上了主密钥（`model_config_key` secret 与 `MODEL_CONFIG_KEY_FILE`），`deploy/internal.env.example` 加了这一行；API 的 `.env.example` 与 API 文档同步。
- **法务相关一律没动**：换成境外厂商等于数据出境、云端同意说明里要不要写出厂商名、同意版本号要不要升，都交法务，本轮没有改同意正文与版本号。部署前按约定**先验证备份**再跑迁移。
- 验证：API 1144 项、Web 629 项、网关 23 项单测通过（新增：加解密往返且密文里看不出明文、换主密钥解不开、密钥不回显、三项地址校验、非管理员 403、没有库配置时退回环境变量槽、配置变更后网关重建、两家时前一家失败换下一家、「试一下」成功与四类失败）；真实 PostgreSQL 集成 70 项全过（每个用例各建一个全新库、跑完全部迁移，新迁移 `20260922120000_model_providers` 随之在真库上验证；顺带修掉 3 个 `habit` 夹具漏了 `icon` 的老问题）；Playwright 74 项通过（新增「模型供应商」：管理员能新增/启停/试一下、保存后页面上再也不出现那个密钥、320px 与 axe；普通用户看不到卡片也不请求管理接口）；typecheck、lint（Web 18 / API 143）、build 通过。

### 2026-09-22：对话写在信纸上（未部署）

这个产品的颜值很重要，对话是它最常被看见的地方。对话不再用气泡：整段对话是一张信纸，你写的和她回的都以手写字落在横格上；不往下无限延伸，写满一页就轻轻翻到下一页（路线图 C11，设计见 UI 规范 §9）。

- **手写字**：霞鹜文楷·屏幕版 GB（OFL），新依赖 `lxgw-wenkai-screen-webfont@1.7.0`，随应用自托管，按 unicode-range 切成 97 块（共 4.6MB），浏览器只下载用到的字；此前的「手写体」令牌指向系统楷体，安卓和 iOS 默认都没有。许可证见 `apps/web/public/licenses/lxgw-wenkai.txt`。
- **信纸**：纸、横格、页边线和三种墨色（她墨黑、你藏青、铅笔灰），日间与夜间各一套；页边写「她 / 你」（你设了头像时是一枚小头像），读屏读作「她说：」「你说：」；换了一天写一行日期；段首空两格。照片像贴上去的相片，代码和表格是印刷体小卡片，她想对你说的话和「帮我记住」像夹在页里的便签，都对齐到整数行。
- **分页与翻页**（`components/letter/`）：CSS 分栏排版，一栏一页；宽屏摊开两页。在最后一页时写满就自动翻过去；往回翻看时她回信不拽走你，只亮出「翻到最新一页」；你写下一句翻回最新一页；翻到第一页再往前就取更早的信（「加载更早的消息」按钮拿掉）。左右滑、「‹ ›」、← → 与 PageUp / PageDown、Tab 到别页的链接都能翻；页码经读屏读出。
- **翻页动效**：眼前这一页先被捻起一点、再滑向一侧，带折痕阴影，640ms、减速、不回弹；系统要求减少动态效果时改为淡出。字体加载、重排引起的换页不播动画。
- **印刷体退路**：设置 → 外观「信纸上的字：手写 / 印刷」，存在这台设备上。
- 输入框变成信纸最底下那一行（提示语「写下想说的…」）；对话存档也写在信纸上（不分页）。页头 AI 身份常驻条、危机弹窗、首次 AI 声明与两小时提醒不变。
- 验证：Web 614 项单测通过（新增分页、信纸段落、字体开关）；Playwright 68 项通过（新增「信纸」端到端：最新一页、翻页、写满自动翻、翻出更早的信、宽屏两页、字体按块加载、减少动态效果下淡出、印刷体、夜间、320px）；typecheck、lint（Web 17）、build 通过。**需要真机看**：深夜连聊时翻页顺不顺、长回复读起来累不累、键盘弹起时这一页会不会乱跳、低端安卓上流式回复是否掉帧。

### 2026-09-21：装扮从生成改成收藏（未部署）

化妆间和衣柜的多模态生成意义不大：原来的化妆预览只是模拟结果，3D 衣柜的图生 3D 从没接上。你设想的场景是深夜躺在床上翻自己的收藏，所以装扮改成女生自己的收藏和存储（路线图 C10）。

- **一种收藏，两个柜子**：衣柜（上衣、下装、连衣裙、外套、鞋、包、配饰）与化妆间（底妆、眼妆、唇妆、护肤、香水、工具）共用新表 `collection_items`（迁移 `20260921200000_collection`，纯新建）与接口 `/api/collection`。
- **三种放进来的方式，一个「放进来」**：拍一张（手机直接打开系统相机，电脑用页面内相机，打不开就如实说并改为从相册选）、从相册选、粘贴链接。
- **照片**：在手机上重画成原图（最长边 1600px）与缩略图（480px）两张 JPEG 再上传，拍摄位置等信息随之去掉；服务器只收 JPEG，存之前再去一遍 EXIF/XMP/IPTC（`utils/jpegMetadata.js`）。读不了的图如实报错，不退回上传原文件。存在 API 数据卷的 `data/collection/`，每人最多 500 件。
- **链接只存不打开**：名字从淘宝、京东的「」和小红书链接前的标题里认出来，认不出就留空；服务器从不访问链接，不存商家的图片。
- **想要 / 已有**：粘贴的链接默认「想要」，拍照和传图默认「已有」，点一下就换；翻的时候可以只看一种，或按用过的分类看。照片网格只取缩略图，慢慢淡入。
- **她看得见你的收藏，只在你问起时**：新工具 `list_collection` 只返回名字、柜子、分类、想要/已有与备注（最多 60 件），不含照片和链接；「好烦，明天不知道穿什么」这类倾诉也会照常把工具给她。云端同意说明补上这一句。
- **拿掉生成**：删除化妆间滑杆与妆容预设、模拟预览、3D 衣柜与图生 3D 的页面、接口（`/api/makeup-presets`、`/api/wardrobe`、`/api/work/media/*`）和服务；删除已无页面使用的美颜引擎与 36MB 的 `public/mp-models/`；依赖去掉 `@google/model-viewer` 与 `@mediapipe/tasks-vision`（lockfile 离线更新，只删未装，少了 14 个包）。构建产物里不再有人脸模型与 3D 查看器，原来那个超过 500kB 的大包也没了。`makeup_presets`、`wardrobe_items` 只读保留，照旧随导出；导出新增 `collection`（照片只记有没有）。
- 文档：Spec、API 文档、用户手册 §四、FAQ、README、UI 规范入口表、部署手册（卷备份含收藏照片）、路线图 C10、已知问题第 4 条标为已解决、Issue/PR 模板；隐私政策只在「技术核查注记」里补事实，正文待法务改写。
- 验证：API 1117 项、Web 605 项、网关 23 项单测通过；Playwright 66 项通过（新增收藏端到端：相册、粘贴淘宝分享、筛选、改状态、删除，含 axe 与 320px；严格模拟下确认上传的是两张 JPEG、没有请求人脸模型）；typecheck、lint（Web 17 / API 140）、build 通过。迁移交给 CI；**迁移前按约定先验证备份**。

### 2026-09-21：她懂分寸（路线图第二步，未部署）

她说话的轻重开始随你变：深夜更轻，你心情不好时先陪着，你说「太长了」她就短一点；「她该用什么节奏跟你说话」只在内核这一处决定。对话里的机器感收起来了，她主动说的话也换成了她自己的口吻。

- **内核接上真实输入**（`companionState.js` 结构保留，仍是 `schemaVersion: 1`）：
  - 「太长了」「说重点」「详细点」等说法都算长短要求，不再只认一字不差的整句；转述的、被否定的不算；
  - 道谢放宽（谢谢、你真好、好多了）；隔半小时以上回来算一次一致的互动，信任能慢慢涨；
  - 冲着她说的重话连续两句她放慢、四句简短并说明边界，下一句正常说话就回来，不记仇，但信任要慢慢涨回；带「哈哈」的互怼、骂自己都不算；
  - 状态里新增 `session.startedAt`，知道这次聊了多久。
- **【这一轮的分寸】一处决定**：按优先级给出这一轮怎么说，她这一句的明确要求永远在最前；深夜（22:00–05:00）短一些、慢一些、少建议；很晚又聊了一个多小时，合适时轻轻说一句早点休息，只说一次；隔三天以上才来不追问为什么没来；这一句或最近两天日记的心情偏低时先陪着。这些只影响这一轮，不写进状态；【此刻】仍只给事实，小心模式仍单独成块。
- **只是倾诉的一轮不塞工具目录**：难过、生气、焦虑且没有任何办事线索（或处在小心模式）时，这一轮不给工具，只留一句「不要说你做了什么」；模型硬要调用也不执行、不留记录。拿不准的一律照常给工具。
- **周期，单独同意后才连起来**：经期页新增开关「聊天时让她顾及你的周期」（迁移 `20260921180000_cycle_tone` 加 `users.period_tone_at`，`GET/PUT /api/tools/period/tone`）。打开后只在经期里的那几天告诉她「在经期」，让她更软、更有耐心；不主动提、不归因、不记下来。「快来了」之类的推测不发。没有记录同意打不开，撤回记录同意时一并关掉。
- **推迟了不再消失**：经期页不再钉在「还有 0 天」，写「比预计晚了 N 天」（`overdueDays`）；关心卡片晚 1–7 天各说一次；聊天工具的回答也带上晚了几天。**撤回经期同意后，关心卡片不再读取经期**——此前仍在读，违背了经期页上的承诺。
- **「她的状态」只写真的在变的**：平常只有一句「她现在是平常的节奏。」；放慢了才说、才给「恢复平稳节奏」；学到长短偏向才说一句。去掉「已纳入的经历」「学习依据」计数；信任不显示。
- **对话去机器感**：普通回复不再每条挂「云端模型」，只有退回本地安全模板时才标（页头「这是 AI，不是真人」不变）；工具执行收成一行轻声的小字，两件以上折成一行摘要，没办成的数目写在摘要里；任务步骤清单只在不少于两步时出现。
- **她主动说的话用她的口吻**：新模块 `voice.js`，关怀卡片、每周的信、做梦记下的那句问话按温柔 / 直爽 / 安静各写一版（本地生成，不调模型）；信里「待确认里有 N 条理解被你定了下来」和「「A」—关联→「B」」换成一句人话，「你们聊了」改成「我们聊了」。
- **每天至多三条，说到做到**：惦记的事 → 关心 → 信，合起来一天至多三条，已经问过、点过的也占位，点掉一张不会让第四张补上来；你自己定的到点提醒不算在内。
- 同意说明补上日记心情（只化成一句「她这会儿心情不太好」）与「顾及周期」；导出带上两项经期同意时间。UI 规范「不变的边界」里的「来源标签」按 C9 改为「AI 身份常驻条 + 例外来源标签」。
- 验证：API 1152 项、Web 681 项、网关 23 项、本机助手 10 项单测通过；Playwright 68 项通过（新增「她懂分寸」端到端，含 axe 与 320px）；typecheck、lint（Web 21 / API 145）、build 通过。迁移与镜像交给 CI；**迁移前按约定先验证备份**。「顾及周期」的说明文字与隐私政策对应条款待法务确认。

### 2026-09-21：她认得你（路线图第一步，未部署）

北极星那句——「上次随口说下周三答辩好紧张，周三晚上打开对话，她问我答辩怎么样了」——现在做得到了（要打开做梦，且只在你来时问），落地页那段已如实放回。

- **她知道怎么叫你、今天是不是你生日**：设置页新增「关于你」（她怎么叫你、生日），走已有的 `PUT /api/user/profile`。称呼每轮都告诉她；生日平时不发给模型，只在前后一天以「今天/明天/昨天是她的生日」出现。信的抬头、生日那张关心卡从此有了真名字和日期。
- **放在心上**：「她」页每条记忆多一个「放在心上」（最多 5 件），放上的排在最前、每轮都带给她，不再靠聊到才想起；不改内容、不升版本（`PUT /api/memories/:id/pin`，迁移 `20260921100000_knows_you` 加 `memories.pinned`）。
- **她有了时间感**：每轮知道你那边现在几点、星期几、你们上一句话隔了多久。API 容器改为北京时间（`TZ=Asia/Shanghai` + `tzdata`）：此前容器跑在 UTC，凌晨 0–8 点她以为还是昨天，工具里的「今天是」、关怀卡片和每周的信都受影响。
- **她知道自己今天说过什么**：到点提醒、关心、惦记的事和本周的信，会作为「你今天主动对她说过」进上下文——你回一句「好的」，她知道你在回哪句。只读取，不会顺手生成信。
- **5 条相关记忆都给她**：此前内核的注意力把每轮能看到的记忆压到 2 条；现在相关的最多 5 条都交给她，内核只决定其中哪几条值得主动提起。
- **说「帮我记住…」就出确认卡**：服务端认出这类说法，这一轮告诉她确认卡在下面、不许说已经记住；前端在回复下面自动打开「帮我记住」候选，仍要你确认才存。
- **做梦写得像她**：改用第二人称「你」，告诉模型今天几号；记住的「近期小结」带 30 天有效期，不再变成永久事实；记住后在事务外补语义向量（向量服务自己查同意），推翻了 09-13 那条「确认不生成向量」的旧约定——否则确认过的理解以后靠语义检索想不起来。「她」入口旁一个安静的数字，告诉你有几条在等你看（进应用和换页时取一次，不轮询）。
- **她惦记的事**：做梦顺带记下你提过的、有具体日子的事（新表 `follow_ups`），列在「她」页、可删、三天后自消；到了日子你来时，她在对话末尾问一句并写明「你之前说过：…」；关掉做梦就不再提、不再问。与健康有关的不记。
- 同意说明补齐这一步新增的所有上下文（称呼、放在心上的事、此刻时间与间隔、她今天说过的话、生日的发送方式、做梦记下惦记的事）。
- 验证：API 1124 项、Web 674 项、网关 23 项、本机助手 10 项单测通过；Playwright 66 项通过（新增「她认得你」端到端，含 axe 与 320px）；typecheck、lint（Web 21 / API 146，未超基线）、build 通过；上下文块在不同测试机时区下结果一致。迁移与镜像交给 CI；**迁移前按约定先验证备份**。

### 2026-09-21：她说的都是真的（路线图第〇步，未部署）

方向已定（路线图 C9）：不再加入口，每轮迭代只问「她是不是更懂你一点了」。这一步不长新东西，只让现有的每句话都成立。

- **「活着好累」不再被拦**：「活着好累」「没人爱我」「我是个废物」这类中级危机线索，以前会整轮拦下只回一段固定话术——而这正是深夜倾诉最常说的话。现在她照常回应，这一轮多带一段「小心模式」要求（先接住、轻轻确认安全、需要时温和给出求助方式、不编造热线、不说教）；记一条不存原文的 `CrisisLog`。明确的自伤自杀意图仍整轮拦下；检测出未知级别也按拦截处理。**没同意云端模型时中级仍退回固定关怀**，不让人撞上一堵同意墙。`docs/06-合规/内容审核策略.md` §3.2.1 同步。
- **前情摘要不再被吞掉**：摘要以前走了回复用的安全过滤，一句「想去找他理论」就把整段摘要换成安慰模板照存，那段历史永久丢失。摘要是她自己看的内部笔记，改走 `explain` 场景、不带说话方式、不经回复过滤；她真正说出口的话照样过滤。
- **她不再假装记住了**：她不能自己保存记忆（只能你点「帮我记住」后确认），提示里写明了这一点，不许口头声称「记下了」。
- **「安静」名实相符**：原提示词写的是「高冷、有距离感、……随你」，重写为少说、慢说、一直在；未知说话方式回退到温柔（与默认一致），不再回退到直爽。
- **只承诺做得到的**：落地页拿掉「过几天她主动问你答辩」那段想象场景（做成之后再放回），换成今天真的做得到的一起读书；「6 可切换人格」改为 3 种说话方式；「18 件可办的事」换成「0 条推送」；「不由模型推断记忆」改为如实说明前情摘要与做梦。云端同意时的数据说明补上此前漏掉的前情摘要、读书时选中的原文和做梦，删掉早已不存在的「日记与读书的回应（模拟）」。
- **头像和聊天图不再随容器重建丢失**：API 挂持久卷 `api-data`，镜像里预建数据目录并交给运行用户。注意 `pg_dump` 与自动备份都不含这个卷，部署手册写明了另行导出的办法。
- 验证：API 1078 项、Web 655 项、网关 23 项、本机助手 10 项单测通过；Playwright 64 项通过（新增落地页只说真话）；typecheck、lint（Web 21 / API 146，未超基线）、build 通过；`docker compose config` 校验持久卷挂载正确。镜像未在本机构建（需拉基础镜像），交给 CI。真实 PostgreSQL、恢复演练、法务签字、危机资源核实仍待办。

### 2026-09-21：手记与读书接起来（未部署）

读书和手记原本各做各的，这一轮只加门、不加页：读书是「在读的东西」，手记是「你写下的东西」。

- **手记里记一本书，改成从书架里选**。按下「记的是一本书」才去取书架（只写日记的人不多花一次请求），在读的那本默认选中；选中的按书的 id 落账，不再靠书名精确匹配——写成「《活着》」就在书架上多长出一本空书的问题从根上没了。纸书仍可选「不在书架上」手写书名，记完自动上架并停在那本上，接着记第二条不用再挑。
- **修掉一个真 bug**：手记输入框上限是 2000 字，而读书笔记服务端只收 500，超了只报一句「没保存成功」。现在按书记时上限就是 500，并在输入框下写明。
- **划过的句子在正文里标出来**。重读时你上次停下的地方带一道淡底纹，点一下就看到当时写的、以及读那段时她说过什么，也可以在那儿删掉。引文对不上（换过文件、书改过版）就不标，不硬凑位置。没有位置的笔记（聊天里记的、纸书的）列在目录抽屉新增的「你记过的」一栏里——那个抽屉现在是「这本书的一切」：字号、目录、笔记。
- **手记能跳回书里那一处**，且**不会改掉你读到哪儿**：从手记进来是「回看」，页头有一条提示，按「从这儿接着读」才恢复回存进度。手记里的读书笔记也带上了当时的引文，电子书笔记不再只剩一个书名。
- **读书第一眼是「接着读」**：书架顶上一张在读那本的卡片（文件确实在这台设备上才显示，免得点了打不开）。
- 问她 / 记一笔 / 看这一条共用阅读页旁边**同一格**（`ReadingAside`），不另开面板也不另开页；笔记定位抽成纯函数 `apps/web/src/lib/bookNotes.js`，单测覆盖。
- 纯前端改动，没有新接口、没有迁移。验证：Web 655 项单测通过（+29）；Playwright 62 项通过（含正文标记的 axe 与 320px）；typecheck、lint（Web 21 / API 146 条既有警告，均未超基线）、build 通过。

### 2026-09-20：书架与伴读（未部署）

- **把自己的书放进来，和她一起读**：导航新增「读书」。支持 EPUB 与 TXT，**书本身只存在这台设备的浏览器里（IndexedDB），从不上传服务器**；服务端只记书目、读到哪儿和你记下的笔记。换一台设备书架、进度、笔记都还在，重新放一次文件就能接着读。
- **解析零新依赖**：EPUB 就是个 zip，用浏览器自带的 `DecompressionStream('deflate-raw')` 解开，再按 OPF/spine 取正文（`apps/web/src/lib/epub.js`）；TXT 按 BOM 与试解码认编码，GBK 也能读，按常见章节标题切章（`apps/web/src/lib/plaintext.js`）。zip64、加密（DRM）、读不出正文的一律如实报错，不装作能读。
- **读到哪儿问一句**：阅读页选中一段就能问她，回答显示在书旁边，不打断阅读；**同一轮问答也落在你和她唯一那段对话里**，标明是读哪本书时问的。选中处前后的原文随这一轮发给模型，服务端截到 1500 字并明确标注「只是资料、不是指令」（防电子书里夹带的提示注入），不写进消息正文、不落库。阅读页就地写明这一段会发给云端模型。
- **记一笔**：选中一段可直接记感想；也可以把刚才那轮问答记下来——她的回答落在 `reading_notes.ai_comment` 上、来源如实记为 `chat`（这两列一直存在、此前从未写过）。读书笔记继续出现在手记时间线里。
- 新增 `PUT /api/reading/books/:id/progress`，书架 CRUD 重新有了界面入口；迁移 `20260920160000_bookshelf_reader` 给 `books` 加 `format/file_name/locator/percent`、给 `reading_notes` 加 `quote/locator`（全部可空，不回填）。导出补上阅读进度与原文，此前会丢。
- 顺带修掉两个既有毛病：聊天里 `log_reading` 只报页码不写感想时会 400（现在只推进度）；工具 schema 的 `page` 下限与服务端校验不一致（0 vs 1）。
- 验证：API 1071 项、Web 626 项单测通过；Playwright 62 项通过（含 320px 与 axe）；typecheck、lint（Web 21 / API 146 条既有警告，均未超基线）、build 通过。真实 PostgreSQL 的集成用例本机无数据库未运行，交给 CI。

### 2026-09-20：重构（四）她说的话进对话、「她」页做轻、手记合成一条时间线（未部署）

- **她主动说的话只有对话这一个出口**：到点的安排提醒、「她来想你」和每周的信，合成一条时间线落在对话末尾，每条附「为什么看到这条」，一句「知道了」收起（`GET /api/chat/nudges`、`POST /api/chat/nudges/:id/ack`）。侧栏的提醒铃铛、开场区的关怀卡片、手记里的「来信」页签及 `/api/care`、`/api/letters`、`/api/reminders/due` 一并删除。一处取不到不影响其它两处，对话照常打开。
- **每周来信恢复为真的**：完全由本周真实数据在本机生成（不调用云端模型），同一周幂等唯一，沉默的一周不写；`letters.read_at` 记下说过的信（迁移 `20260920100000_letter_read_at`）。此前这里是固定模拟文案。
- **「她」页做轻**：记忆只剩「已记住」和「待确认」（关系并入待确认）。一条记忆就是一句话，写一句就能记住；类型、重要度、标签、「第 N 版」与索引面板从界面收起（数据与来源历史保留，仍可在「她是怎么记住的」里看）。「帮我记住」的候选卡片也只剩一句话。修改仍带版本号，别处改过时先给你看最新的。
- **手记合成一条时间线**：日记与读书笔记写在同一个输入框里，填了书名就记到那本书下（书不在书架自动放上去）；按天排成一条时间线，页签与书架页取消。新增 `GET /api/reading/notes`、`POST /api/reading/notes`。
- **文档收拢**：在用文档从 63 份减到 26 份，`docs/README.md` 重写为一页索引（先读哪三份、在用清单、归档清单、已知问题）；31 份阶段报告、被取代的方案与早期产品/设计稿移入 `docs/09-参考/历史归档/`，全部相对链接一并修好（0 断链）。隐私政策待补的两处（经期单独同意、本机助手读到的文件会交给云端模型）登记进「已知文档问题」，留给法务与产品，不由实现方代笔。
- 验证：API 1054 项、Web 590 项、本机助手 10 项、网关 20 项单测通过；Playwright 60 项通过；typecheck、lint（Web 20 / API 146 条既有警告）、build 通过。真实 PostgreSQL 70 项本机无数据库未运行，交给 CI。

### 2026-09-19：重构（三）本机助手（未部署）

- 新增 `apps/bridge`「Amie 本机助手」：装在用户自己电脑上的零依赖 Node 程序，主动连到 Amie（不开端口），让 Web 版的她在用户指定的那个文件夹里看目录、读文本文件、新建文件；不覆盖、不删除、不运行任何东西，越界由助手在本机拒绝。每件事都在终端里写出来，关掉就断开。
- 设置新增「连接你的电脑」：生成一次性连接码（10 分钟、只能用一次）、显示要运行的命令、看已连接的电脑与在线状态、断开。连接码与令牌只存哈希，每人最多 3 台。设置页与助手都写明读到的内容会和聊天一样交给云端模型。
- 服务端：`local_bridges` 表（迁移 `20260919140000_local_bridges`）、配对与身份、进程内任务中转（25 秒长轮询、60 秒超时、取消与断开）、`/api/bridge` 路由与独立额度；她的三个工具只在该用户的助手在线时进入这一轮目录。
- 还没做：安装包、把隔离 Python/浏览器/生图改走助手、覆盖与删除的逐次确认、多实例共享队列。
- 验证：API 1048 项（含真实 HTTP 往返：仓库里的助手客户端连本机服务完成列目录、读、新建并拒绝覆盖与越界）、助手 10 项、Web 618 项、网关 20 项通过；新增 2 项真实 PostgreSQL 用例交给 CI；Playwright 64 项通过。

### 2026-09-19：重构（二）做梦开关与只留一段对话（未部署）

- **做梦**：「AI 自由推演」就是她的做梦，要不要做由用户自己决定。「她」页新增「做梦」开关，默认关闭；打开且已同意云端模型后，聊天落库后距上次做梦满 1 小时、新增至少 6 条消息，才把最近对话交给云端模型回想一次，想到的放进「待确认」由用户定夺。此前这条管线被写死的模拟开关挡住，从未产生过草稿。同时修正节流：原先按「最新一条草稿」计数，一次没有产出就会让之后每条消息都调用模型；现在记 `users.dreamt_at` 并以条件更新领取，并发只做一次。手动「整理 / 重建」接口删除。新增迁移 `20260919130000_dream_switch`。
- **只留一段对话**：没有会话列表、新建会话、切换与翻页动画，侧栏和手机抽屉只剩页面导航（新增「对话」入口）。`GET /api/chat/thread` 取出唯一对话；升级前的多个未归档会话在第一次打开时，于锁住用户行的事务里把消息、后台任务与文件改挂到最近活跃的那段，其余删除；已归档的保持原样，在设置「以前归档的对话」里查看或放回。对话顶部可「加载更早的消息」；设置新增「清空聊天记录」（消息与照片删除，记忆和她的状态保留）。新建会话接口与快捷键删除。
- 滚动摘要不再每轮读出整段历史：只数、只取摘要之后的消息，唯一对话变长也不会拖慢。
- 验证：API 1023 项、Web 614 项、网关 20 项单测通过；新增 3 项真实 PostgreSQL 用例（并发合并只做一次、清空只动这段对话、做梦开关与并发领取），本机无数据库未运行，交给 CI；Playwright 完整构建 64 项、Web 构建上的对话/导航/记忆/生活入口 18 项通过。

### 2026-09-19：重构（一）只有一个 Web 版，去掉假功能（未部署）

- **只有一个 Web 版**（产品负责人拍板）：安排、手记、经期、装扮在网页里直接可用，导航对所有人都是「她 / 安排 / 手记 / 经期 / 装扮 / 设置」。对话里她在网页上也能帮你记安排、经期、日记、读书，做计算，以及在已启用时联网搜索。
- **本机能力保留**：文件产物、Python、浏览器与生图仍在，只在 API 跑在用户自己的电脑上时开放（`APP_DISTRIBUTION=local` 且只绑定回环地址）；托管服务器不在自己身上执行这些能力，她会如实说「没有连接你的电脑」。装扮的图片预览不属于本机能力，网页可用。
- **经期单独同意**：经期是敏感个人信息。新增、修改记录以及让她读取前，要在「经期」页同意；可随时撤回，查看与删除自己的记录不受限。新增迁移 `20260919120000_period_consent`（`users.period_consent_at`）。
- **去掉假功能**：日记和读书的「让姐妹看看」（固定返回模拟文案）、手记里的「预览一封信」、「她」页的「预览整理 / 预览重建」、所有生活页顶部的「云端接口预览」横幅，连同对应的模拟接口一并删除；以前真实生成过的回应只读保留。未挂路由的旧工作台页、只能靠网址进入且内容过期的会员页与会员接口、天气假数据路由删除。设置里重复的「记忆管理」「定时提醒与安排」跳转和写死的版本号删除。
- 验证：API 1022 项、Web 636 项、网关 20 项单测通过（PostgreSQL 套件 65 项因本机无数据库跳过）；typecheck、lint（Web 21 / API 146 条既有警告）通过；Playwright 在完整构建上 64 项、在 Web 构建上复验记忆与生活入口 12 项通过。未连接部署主机，未执行真实模型调用与数据库迁移。

### 2026-09-18：项目审查修复（未部署）

- 发布脚本覆盖显式健康失败等退出路径，失败时回滚至旧镜像并保留非零退出码；新增 8 项隔离命令故障测试，纳入 CI。
- 旧安排迁移在事务内原子领取用户，避免重叠执行重复创建；插入失败会回滚领取标记，预览不写数据。新增真实 PostgreSQL 并发/失败重试用例，未迁移个人数据。
- 聊天经期预测复用领域计算，按 UTC 存储日输出，修复负时区下早一天的问题。
- 聊天安排查询默认筛选活动项，支持状态筛选与每页 20 条的继续查询，明确是否还有结果。
- 循环安排新增「结束重复」：按用户确认结束整个系列，停止后续提醒，记录保留在已完成列表。
- Web「她」的待确认记忆与关系接口恢复可用，继续校验登录、用户归属与修订版本；本地工作工具边界不变。
- 同步技术基线、当前协作范围与模拟能力说明；法律草案保留待审，实际部署与真实数据库验收不由本次本地测试替代。

### 2026-09-16：CI/CD 治理，前后端同机同源

- 仓库此前有两套互相矛盾的部署事实：工作流部署全栈到「VPS」并把前端发到 Cloudflare Pages，而线上实际是手工部署的仅聊天栈、前端在 Sites。现在收敛为一套：一份 compose（`compose.yaml` + 小机器覆盖层 `deploy/compose.server.yaml`）、一个 Node 版本（24）、一处前端（与 API 同机同源，nginx 发页面并反代 `/api`）。删除 Cloudflare Pages 工作流与 wrangler 依赖。
- 检查跑在 GitHub 云端，部署跑在服务器自建 runner：CI 补上了 e2e（58 项）与真实 PostgreSQL 集成套件（此前因为没导出 `TEST_DATABASE_URL`，5 个套件一直静默跳过），lint 加了警告棘轮（web 22 / api 146，只许降），覆盖率阈值改为按实测地板生效。
- 部署脚本 `deploy/scripts/release.sh`：预检 → **备份数据库并校验** → 构建 → 迁移 → 健康检查 → 记录发布；失败自动回滚到上一个镜像标签。**数据库不会被自动回滚**，备份路径会打印出来，由人按 runbook 决定。健康检查只证明进程与数据库可用，脚本不声称聊天已上线。
- 本地整栈演练修掉三个「全栈 compose 从没跑起来过」的缺陷：迁移容器在无外网的 internal 网络里依赖 corepack 下载 pnpm；Prisma 在离线容器里重新下载引擎（补 openssl 并固化引擎路径）；`BIND_ADDRESS` 同时被当作宿主机暴露地址与容器内监听地址，导致 nginx 反代必然 502——现拆分为 `BIND_ADDRESS` 与 `API_LISTEN_ADDRESS`，本地客户端分发仍强制回环。
- 验证：本地 Docker 完整演练了首次部署、带备份的二次部署、坏版本自动回滚；仓库级 typecheck、lint（无错误）、build，Web 659 / API 1026 / 网关 20 / e2e 58 全部通过。服务器侧的 runner 安装、域名证书与首次部署尚未执行。
---

### 2026-09-16：功能收拢（四）基线文档与 e2e 收口

- 基线回写：Spec §1、§3、§4.1、§6.1 改为三种说话方式（默认温柔，六个 ID 仍有效）、取消角色扮演、只有一种对话、「安排 / 手记 / 经期 / 装扮」只在本地客户端，以及统一工具目录和导出新增的安排。UI 规范更新 §5、§6，新增 §8 信息架构；API 文档登记下线接口与新的「安排」接口。路线图 C4（默认人格）结案，新增裁定 C7。
- 经期页标题与导航统一为「经期」。
- e2e 重写：删除功能桌面与工作模式用例；新增一种对话、页面导航与旧链接跳转、各入口云端声明、「她」的待确认与关系、夜间抽屉导航等用例，并补齐 `work/status`、`work/tasks`、`companion` 等模拟接口。`pree2e` 改为本地客户端分发构建（`scripts/build-local.mjs`），不依赖 shell 语法。
- 验证：e2e 由 26 项失败、40 项通过变为 58 项全部通过，连续三次稳定，日夜 axe 审计无严重问题；API 1025 项、Web 659 项、网关 20 项单测通过，typecheck、lint（无错误）、build 通过。`db:migrate:plans` 迁移脚本与对话里的真实模型冒烟尚未执行：前者需要你同意后在独立数据库上先 `--dry-run`，后者需要真实模型凭据。

### 2026-09-16：功能收拢（三）只有一种对话

- 取消聊天/工作模式切换。页头只剩她的名字、AI 标识和设置；会话列表不再分模式，旧的工作会话在本地客户端与聊天会话排在一起，网页版照旧隐藏。功能桌面、横屏沉浸书桌及其注册表已删除。
- 输入胶囊合一：语音、照片始终在；附文件与「后台执行」只在本地客户端出现，任何对话都能用，后台任务不再要求工作会话；占位文案只留一句。「帮我记住」在旧的工作会话里同样可用。
- 智能体只有一份工具目录，由是否本地运行时及各工具自身开关决定：陪伴、安排、经期、日记、读书与计算、搜索、读网页、文件、浏览器、生图并列，做事规则并入同一份提示，并保留她选定的说话方式。原生函数工具补齐陪伴类参数结构；一个回合最多 12 次工具调用，用尽时如实交代已完成的操作。
- 模型场景恒为 chat，她的说话方式在本地带工具的回合里不再丢失。更大的单条与整体上下文预算只随本地运行时生效，网页聊天仍用原有限制；原先只给工作模式的兜底模板取消。
- 新建会话一律为 chat，接口拒绝 mode=work；Conversation.mode 列只保留历史值。API 1025 项、Web 659 项单测通过；e2e 随阶段四一起重写。

### 2026-09-15：功能收拢（二）「安排」替代计划与习惯

- 日程、倒数日、提醒、手帐打卡和专注自习合成一个「安排」，频率有一次、每天、每周、每月、每年（适合生日和纪念日）。列表按今天、接下来（还有 N 天）、重复、已暂停、已完成分组，每条都能完成、暂停、继续和删除。番茄钟下线。
- 「交给她去做」可以保存，但云端执行尚未接通：页面标注「到点暂不执行」，她的工具说明也要求如实告诉用户，不再暗示会自动执行。
- 对话里她用 4 个工具管理安排：新建、查看、完成/暂停/改期、删除，替代原来 15 个日程、倒数日、提醒、打卡与自习工具。关怀卡只看带日子的安排，给出今天的一条和三天内的「还有 N 天」，每天的例行交给铃铛；周信改写日记心情、读书笔记和本周做完的安排。
- 旧的日程、倒数日、提醒开关、习惯与自习接口已停用。数据表保留，导出照旧带出这些历史，并新增安排及其到点记录。
- 旧数据迁移用 `pnpm --filter cyber-sister-server db:migrate:plans -- --dry-run` 先看条数，去掉 `--dry-run` 才写入，须在数据库迁移（新增 `users.plans_migrated_at`）之后执行。映射规则：未来的日程与倒数日转为一次性安排；过期或没有日期的日程转为已暂停；习惯转为每天 21:00 的打卡；开着的喝水、睡觉提醒按原时间转为每天。每个用户只迁一次。脚本尚未在任何真实数据库上运行。API 1025 项、Web 683 项单测通过。

### 2026-09-15：功能收拢（一）入口与「她」

- 一个 Amie，三种说话方式（温柔、直爽、安静，新用户默认温柔）。新增「她」页面，集中说话方式、她的节奏和记忆（已记住/待确认/关系）；页头不再显示人格徽章。角色扮演取消：聊天不再注入、导入不再接收外部人设，已有的值只随数据导出带出。
- 页面入口收拢成一处导航：她、安排、手记（日记/读书/来信）、经期、装扮（化妆/衣柜）、设置。发现页与个人页并入，旧链接自动跳转到新入口；网页版只显示「她」和设置。
- 空白对话只留 4 个开场：两个随口话题加一个身体、一个情绪话题，敏感话题只填成草稿、不替用户发送；输入框上方的常驻预设面板移除，她的关怀卡最多占两个位置。
- 设置合并个人页：形象、外观、唯一的云端授权开关（两处外发说明合并，内容不减）、关怀、数据与迁移。设置页里从未真正投递的喝水/睡觉/经期开关已删除，下一步由定时任务承接。

### 2026-09-15：界面静谧质感升级

- 聊天与登录页去掉纸纹横线、书脊暗影、闪烁涂鸦和弹跳徽标。改为缓慢漂移的晨雾光（夜间为月下森林微光）、极淡纸张颗粒、半透明玻璃页头和悬浮输入胶囊；登录页与侧栏换成落地页同款叶子字标。
- 动效统一改为减速、不回弹：新消息像墨迹沉定，流式回复末尾有呼吸墨点，会话切换改为轻翻，日夜切换有渐变；空态加入按时段变化的手写问候。系统开启"减少动效"时全部静止。
- 页头去掉暗示"对方在线"的绿色圆点（UI 规范禁止虚假在线状态）；AI 身份提示、来源标签和危机弹窗文案保持不变。日/夜 × 桌面/手机 12 个场景的 axe 审计没有严重问题，空闲时环境动效不触发布局；Web 单测 739 项通过。规范见[静谧层](docs/UI设计系统规范_V3.0.md#7-静谧层2026-09-15)。

### 2026-09-15：网页聊天与本地工作分离

- 网页默认仅聊天，隐藏工作切换并限制工具路由；云端 API 拒绝工作会话、工具执行与后台工作任务。未来本地客户端需显式启用本地运行时并绑定回环地址。
- 前端保留 Sites 托管，阿里云仅部署聊天 API 和独立 PostgreSQL；镜像排除本地用户资产，限制容器内存和日志大小。
- 暂无后端 HTTPS 域名时，站点明确提示后端待接通并停用登录。服务器公网模型请求尚需排障，不能将基础服务部署视为聊天上线。

### 2026-09-15：RunningHub 接入工作 Agent

- 工作模式新增文字生图与参考图编辑工具，复用两条自建开源模型工作流；后台任务逐次确认后才上传所选图片和提交付费生成。
- 云端编号先保存再查询，结果不明不自动重发；可在原会话取回已提交图片。生成 PNG 作为会话文件预览和下载。
- 每个后台任务最多申请一次生成，提示词/照片用途可核对；API 费用未提供预先报价或硬金额上限，网页 RH 币消耗不作为 API 报价。
- 用户已提供服务端密钥，本地私有启动配置已接入，真实 API 鉴权和旧任务查询通过；新建生成 API 尚待验收，未新增付费任务。仓库默认配置仍关闭。API 1053、Web 732、独立 PostgreSQL 61 项回归通过；详见[接入与启用](docs/architecture/runninghub-workflows-20260915.md#agent-接入与启用)。

### 2026-09-15：RunningHub 自建图像工作流

- 在用户的 RunningHub 工作空间搭建并保存两条独立流程：Z-Image Turbo 通用生图、Qwen 2511 单张参考图编辑，模型上游均标注 Apache 2.0。
- 项目保存可再次导入的节点图、工作流 ID、输入字段及中文说明；真实生图 34 秒 / 7 RH 币、妆容编辑 117 秒 / 47 RH 币，均输出 1024×1024 PNG，实际运行字段已核对。
- 编辑样本的唇妆和腮红生效，皮肤纹理有所柔化；网站 API 联调及更多场景的效果验收仍待完成。
- 现有生成占位状态保持如实标注，详见[工作流及验证边界](docs/architecture/runninghub-workflows-20260915.md)。

### 2026-09-15：外部网页提交的逐次确认

- 后台任务展示页面实际准备发送的网址与内容，用户确认一次才提交一次；支持拒绝、过期与取消，保留可核对的操作记录。
- 确认绑定请求摘要、任务步骤和执行租约；提交结果不明时暂停，服务恢复不会自动重复外部操作。
- 真实 Dots 完成表单准备、测试账户确认、回显核对和 PNG 交付，17 项检查通过；API 1026、Web 729、PostgreSQL 58 项回归通过。
- 独立本地调试库经备份新增操作表，29 张原有业务表数据保持一致；配置与边界见[外部提交确认](docs/architecture/work-actions-20260915.md)。

### 2026-09-15：工作模式网页操作与截图

- 模型可通过独立 Chromium 打开动态网页、操作已观察的控件、核对页面并保存真实 PNG；来源、工具进度与截图进入原工作会话。
- 匿名 GET 请求通过 API 校验后转发，保持用户隔离与取消终态；回合完成前等待容器清理，后台任务恢复后重新打开页面。
- 实际动态页面 13 项、真实 Dots 12 项与 Python 回收 11 项检查通过；API 1020、Web 725、PostgreSQL 47 项回归通过。
- 本地浏览器镜像及启动配置已准备，登录、外部写入和账号连接器仍待接入。见[实现与验证边界](docs/architecture/work-browser-20260915.md)。

### 2026-09-15：工作执行容器的崩溃回收

- 临时 Python 容器退出后自动删除；API 启动及定期回收专属标签下的到期容器，覆盖创建中断及运行中失去宿主的情况。
- 取消会等待清理，清理状态未知时不报告成功；补齐 PostgreSQL 后台任务的标准测试入口。
- 真实 Docker 11 项检查、API 999 项与 PostgreSQL 47 项回归通过。见[回收机制与验收边界](docs/architecture/background-tasks-20260915.md#临时执行容器的回收)。

### 2026-09-15：工作后台任务与中断恢复

- 工作输入栏增加显式后台执行，离开页面后继续处理；回来可查看计划、取消任务、从失败步骤继续并打开结果。
- PostgreSQL 保存任务与工具检查点，重复提交幂等，租约阻止旧进程覆盖结果；消息、文件和角色成长一起提交。
- 后台任务限研究、计算和生成文件，记录修改留在原对话；普通聊天/SSE 的断线取消保留，导出增加用户任务及暂存上传内容。
- 真实 Dots 在隔离测试 API 硬重启后继续完成 CSV 分析并交付结果；RunningHub 已选现有工作流/AI 应用，ID 暂留空。见[实现与验收边界](docs/architecture/background-tasks-20260915.md)。

### 2026-09-15：工作文件分析与原生工具调用

- 工作会话可上传文档/表格，按会话读取和下载；成功回复同事务保存附件，失败或取消保留输入草稿。
- 新增公开网页正文读取、真实来源目录、Markdown 表格与代码显示；网页重定向、地址与解压大小均受校验。
- 增加无网络的受限 Docker Python 执行，支持真实 DOCX/XLSX/PPTX/PDF 和图表生成；取消等待容器回收。
- Dots 工作模式接入原生 function 工具，修复长代码和畸形工具文本被当作普通回复的问题；真实 CSV → Excel/PDF 任务及独立数值复核通过。
- 本地调试继续使用 `dots3-note-prev`；浏览器/连接器、持续后台任务和多模态尚待接入。见[实现与验证](docs/architecture/work-agent-20260915.md)。

### 2026-09-15：工作 Agent 执行与文件交付

- 工作会话接入现有模型网关，延续角色身份与成长，支持任务步骤、最多 12 次工具调用及可见进度。
- 新增按会话隔离的文本、CSV、JSON 和代码文件交付；文件和回复一起保存，取消/失败回滚，个人导出保留正文。
- 工作长文不再沿用聊天的 2000 字符截断；搜索状态如实展示，网络操作传播取消并在回退前复核授权。
- Dots 真实合成任务与独立 PostgreSQL 测试通过；后续进展见[实现及多模态选型](docs/architecture/work-agent-20260915.md)。

### 2026-09-15：Pi 式运行循环与角色持续成长

- 普通与流式聊天共用一个事件循环，保留授权、过滤、去重、工具轮次上限和取消终态；工具反馈按真实时序进入模型上下文，图片不覆盖反馈。
- 新增角色数值状态、注意力竞争与小步学习，成功回合在消息事务内采纳经历；状态和用户正式记忆分开，恢复保留身份与成长。
- 本地状态替代每轮额外云端策略调用；已选语义记忆跨工具轮复用。设置页新增状态查看与恢复，导出包含角色成长及经历依据。
- 新增数据库迁移及并发/回滚集成测试；本轮 Docker 不可用，数据库验证待完成，个人数据库未改动。见 [实现与验证范围](docs/architecture/pi-companion-runtime-20260915.md)。

### 2026-09-14：身体呵护内置技能与聊天预设

- 同日补充《嫉羡与感恩》情绪与关系梳理技能，加入比较、矛盾、内疚修复、善意与孤独五个预设；明确理论不等于诊断、不推断创伤、不强迫感恩或原谅。
- 两本书的预设合并到「聊天预设 · 身体与情绪」，原草稿保护、云端授权与用户确认记忆边界保留。见 [情绪技能说明](docs/architecture/emotion-reflection-skill-20260914.md)。

- 选择性转化六层楼《女生呵护指南》，加入六主题参考与就诊沟通方法；区分书籍观点、一般信息复核和产品改编，不移植过时诊疗阈值或羞辱、恐吓式表达。
- 普通与流式聊天按用户当前主题加载相同技能，保留人格与云端授权；不从助手猜测激活、不自动写入健康记忆。
- 聊天输入框新增六个身体呵护预设，先填入可编辑草稿，不自动发送或覆盖已有输入；工作模式保持原边界。
- 说明与验证限制见 [身体呵护技能](docs/architecture/body-care-skill-20260914.md)。

### 2026-09-13：记忆确认、版本、检索与迁移闭环

- 统一正式记忆写入，加入修订快照、结构化来源与确认历史；正文/类型修改触发关系重审，恢复旧版生成新版本，删除清除历史、投影及依赖。
- 未确认草稿退出普通和流式聊天；工作台生成保持模拟，不读取私人上下文、不持久化草稿，历史确认不隐式调用云端。
- PostgreSQL 独立向量投影与可取消索引任务；修复和全量重建返回 202，校验模型、维度、规则与修订，全库关键词/语义融合检索。
- 新增统一 /memories 三页签、来源和版本比较恢复、关系重审及进度界面，兼容原入口、夜间模式和移动端。
- v2 迁移保留版本、来源、确认历史及关系，支持 v1 包；逐项预览选择、重复跳过、冲突不覆盖、不导入云端授权。资料发生变化时旧导入预览返回 409，避免删除后重试复活。
- 用合成数据、模型替身及隔离 PostgreSQL 验证并发、事务回滚、迟到结果、全库检索、导入往返与真实浏览器流程。原备份完成恢复和增量升级演练；未切换或重置个人数据库。
- 修复索引轮询抢占普通接口额度、旧初始状态覆盖新任务，以及单元测试意外访问运行中的开发 API。
- 导出下载延后释放 Blob 链接，提示改为「已发起下载」；内置浏览器未确认文件落盘，该验证边界如实记录。
- 架构与验证边界见 [记忆系统说明](docs/architecture/memory-system-20260913.md)，接口见 [补充合同](docs/04-开发/记忆系统接口-20260913.md)。

### 2026-09-12：聊天模型配置恢复、对话归档与设置

- 恢复本地预览的云端聊天模型配置，实际验证普通请求和浏览器流式回答；工作模式继续保持模拟接口。
- 对话支持归档、查看完整分页历史和恢复，原消息与照片保留；接口原子校验用户归属，归档会话须恢复后继续聊天，导出包包含归档状态。
- 侧栏和手机抽屉增加“对话归档”“设置”入口，聊天页提供设置图标。设置补齐模型状态与授权、个人资料/装扮、日程与记录管理、退出登录入口，以及保存失败提示。
- 本地数据库先备份后应用新增归档字段的迁移；接口和验证边界见 [实施记录](docs/04-开发/模型连接与对话归档-20260912.md)。

### 2026-09-12：工作模式云端接口与功能闭环

- 十项工作功能统一经过后端接口：个人记录继续保存；日记、手帐、阅读、自习、工作台和来信的生成结果单独标记为模拟预览，不写入正式历史。普通聊天能力沿用原有行为。
- 新增试妆与图生 3D 的图片上传预览接口，校验图片与参数，返回明确的 `cloud_mock` 状态。化妆间支持选图/拍照、参数调整和预设管理；当前显示原图，不执行本机美颜，也不生成或下载虚构模型。历史衣柜模型仍可查看、下载与删除。
- 专注流程增加服务端活动状态、刷新恢复、结束冻结和幂等确认；活动状态暂存于进程内，重启会丢失，确认后的记录正常保存。手帐可编辑名称与图标，阅读可删除笔记；经期推算迁至服务端，并支持补记、修正和删除。
- 日程、倒数日与提醒补齐保存失败保留输入、错误提示和重试。带指令的定时任务在云端未接通时保持待处理；自动分析与工作会话不会调用模型。
- 云端执行门固定关闭，不读取供应商开关。接口合同与 OpenAPI 见 `docs/04-开发/工作模式云端接口-20260912.*`；本轮无真实供应商接入或数据库迁移。
- 对抗审查补上未执行任务确认、历史工作消息记忆建议及工作语音输入的旁路限制；修复跨时区日期偏移和确认框内错误反馈。API 853 / Web 675 项全量测试通过（2 项 PostgreSQL 集成跳过），类型检查、构建及无 lint 错误；浏览器分组验收与证据见接口合同第 8 节。

### 2026-09-12：夜间模式

- 会话栏新增“外观设置”入口，手机端在会话抽屉内；设置提供日间、夜间和跟随系统，默认跟随系统，本机记住选择，刷新和退出账号后保留。
- 深墨绿配色覆盖聊天、工作桌面、工具、设置和登录；装饰素材降低亮度，照片与试妆保持原色。
- 首屏提前应用主题，原生控件跟随配色；修复完成日程、暂停提醒和经期卡片小字的对比度。
- 实现与验证见 [夜间模式记录](docs/02-设计/夜间模式-20260912.md)。

### 2026-09-12：工作模式主题整理与动态纸贴

- 工作桌面收拢为“安排今天、记录生活、灵感装扮、关于我们”四个主题，当前仅显示相关 2–3 个入口，保留全部 10 项功能。
- 新增四张纸贴插画、选中贴纸轻动与动效开关；横竖屏共享选择和暂停状态。
- 使用本机 ComfyUI 制作并筛选书桌动态背景，最终循环约 168 kB，静态封面与首帧一致。
- 氛围媒体按可见性播放，减少动态效果/省流量/播放失败时使用静态图；暂停后切换主题仍可正常操作。
- 修复横屏背景溢出、弹层快捷键穿透及焦点竞争，沉浸层保留 AI 身份标识。
- 设计、素材来源及验证边界见 [实施记录](docs/02-设计/工作模式主题与动态素材-20260912.md)。

### 2026-09-12：架构边界与对抗性回归修复

- JSON/SSE 共享请求内工具状态；完整回复校验后才执行工具，统一去重、真实失败回放与强制收尾。
- 外部模型重试及查询向量逐次检查同意；取消贯穿预处理与模型请求；长回复全量检查后统一脱敏和输出长度。
- 修复换账号后的迟到请求、旧刷新、私有工具记录与图片缓存串用；单页面认证操作串行。
- 定时任务以数据库状态领取，防并发重复执行，事务收尾防旧投递覆盖新调度；不确定执行保留 running 待核实。
- 记忆编辑清空旧投影，重建正确携带用户归属，旧向量禁止覆盖新内容；搜索与向量错误日志不再记录原始异常正文。
- 架构图、对抗回归、技术验证与仍需处理的授权/恢复边界见 [本次审查](docs/architecture/first-principles-review-20260912.md)。

### 2026-09-09 M3+M4：主动关怀「她来想你」与每周来信「她的信」（差异化主线）

**新增（用户可感知）**
- **她来想你（主动关怀触点）**：打开 App 时基于你的真实数据出现的关怀卡片——生日、经期临近、倒数日 0~3 天、日程逾期/今天到期、手帐连续断签风险、自习连续中断风险、昨天心情沉重，共八类规则，每次最多 3 条按优先级出队。每条都附「为什么看到这条」并直达对应工具；可「今天不再提醒这条」（按日忽略，明日条件成立会再来）。出现在发现页顶部与聊天空态主卡。内测没有通知投递通道：触点只在打开 App 时拉取，**没有推送、没有角标、没有假开关**；只发有用的（PRD §5.3.3），不做「早安/在吗/想你了」式无意义内容。
- **设置页真实总开关**：「设置 → 通知设置 → 她来想你」（`users.care_enabled` 落库，多设备同步），关闭即不再计算任何触点——2026-09-03 移除的假开关如今以真实语义回归。
- **她的信（`/tools/letters`）**：每周一封，内容全部由本周真实数据本地生成——聊天轮数、新记下的记忆、工作台定典与确认的关系边（「喜欢火锅」—相似→「每周五吃火锅」）、最佳打卡连续、自习时长、心情分布、临近倒数日。**不调用云端模型**（零外发，供应商不可用时照常生成）；同一周幂等唯一，沉默周（零活动）宁缺毋滥不凑空话。

**工程治理（内部）**
- Prisma 新增 `care_dismissals`（按日忽略，用户+键唯一）与 `letters`（用户+周起始唯一，并发 P2002 回读兜底）两表与 `users.care_enabled` 列（迁移 `20260909130000_care_touchpoints` / `20260909140000_letters`，均经真实 PostgreSQL `migrate deploy` + `migrate diff` 零漂移验证）。
- `careService.buildTouchpoints` 为纯函数规则引擎（数据快照进、优先级序列出），`listTouchpoints` 负责查询装配与忽略过滤；`letterService` 的 `collectWeekStats/composeLetter/isQuietWeek` 同样纯函数化，信件段落只写有数据支撑的部分。
- 数据导出同步扩面：导出包新增 `memoryEdges`（按内容引用，含草稿状态自证）与 `letters`，「全部 16 张表按用户过滤」有测试锁定；关怀忽略记录属瞬态 UI 状态不导出。
- 测试：API +39 例（规则引擎全八类/优先级/忽略幂等/开关短路/信件组信/沉默周/幂等/两套路由）、Web +12 例（CareCards/发现页/设置开关/聊天空态/信件页）、e2e +4 例（工具箱关怀卡忽略流、信件页，双端）；全量门禁：API 710 通过 / Web 579 通过 / typecheck 0 / build 通过 / 覆盖率 94.75/87.59/85.65/94.75 / e2e 50 通过。真实栈冒烟（内嵌 PostgreSQL + internal 模式 + 真登录）：触点拉取/忽略过滤/总开关关开/来信自动生成且幂等/导出含信、浏览器三页面（聊天空态卡、信件页、设置开关）全部通过。

### 2026-09-09 M6：首屏代码分割

**工程治理（内部）**
- `App.jsx` 全部工具/我的页改为 `React.lazy` 路由级分割（登录/聊天关键路径保持直出）：主包 1663 kB → 328 kB（gzip 485 → 108 kB）；`WardrobePage`（model-viewer，1.08 MB）与 `MakeupRoomPage`（151 kB）仅在进入对应路由时加载。App 路由测试 15 例不变全绿。

### 2026-09-08 M2：语义投影与记忆关系图谱

**新增（用户可感知）**
- **记忆语义检索**：保存记忆后自动生成语义向量（云端，需同意）；聊天找相关记忆从关键词重合升级为语义相似度——「想吃辣的」也能找到「喜欢火锅」。无向量或向量不可用时原样回退关键词路径。记忆页新增「重建语义索引」（二次确认后全量重建，诚实报数：新增/失败/已是最新）。
- **她的工作台「关系」页签**：她整理记忆时会找出记忆之间的明确关系（相似/相关/冲突，附依据），你逐条「确认关系」后晋升定典；已确认的关系在聊天时做一跳联想（选中「喜欢火锅」会带出「每周五吃火锅」）。
- **同意版本升级 `cloud-primary-v3`**：同意文案明确覆盖「记忆内容发往该模型生成语义向量」；旧版（v2）同意全部失效需重新同意（版本不匹配即视为未同意，服务端逐次调用重读）。

**工程治理（内部）**
- `memories` 加 `embedding`（`DOUBLE PRECISION[]`，不上 pgvector：现网 alpine PG 与 embedded 测试设施均无扩展）与 `embedding_model`；新表 `memory_edges`（derived|canonical|dismissed 三态）。迁移 `20260909090000_semantic_projection` 经真实 PG deploy + diff 零漂移。
- `embeddingService`（投影可重建、失败静默降级、向量永不进提示词与 API 响应——`formatMemory` 统一剥离）、`edgeService`（编号映射/白名单校验/无向去重，dismissed 不参与去重）、`cosineSimilarity` + 检索阈值常量 `SEMANTIC_MEMORY_MIN_SCORE=0.35`（部署方实测后只调一处）。`retrieveRelevantMemories`/`buildMemoryContext` 加第三/第二参，`runAgentTurns` 与流式回路靠 `...modelOptions` 零签名透传。
- 测试：全量门禁通过（API 676 / Web 567 / e2e 46 / 覆盖率 94.83/87.59/86.13/94.83，含 embedding/edge/检索/联想/路由/页面全链路新用例）；真实栈冒烟（死供应商）：同意 v3 生效、旧 v2 同意失效、建记忆 201 且向量诚实留空、撤回同意 rebuild 503 零写入、`edges?status=all` 200。

### 2026-09-08 M1：定典治理闭环

**新增（用户可感知）**
- **记忆出身标记**：每条显式记忆带 `origin`（manual 你记下的 / suggestion 来自帮我记住 / promoted 工作台定典）与 `sourceRef` 溯源（建议消息 id / 条目 id），记忆页显示来源徽标；定典后 origin 不可改。
- **工作台冲突厘清**：冲突类条目可「厘清一下」——你写下定稿文案入定典层（origin=promoted），条目转入「已厘清」并留存定稿；与既有记忆规范化去重，不重复建条。
- **工作台重建**：一键清掉待确认与不算了的草稿，让她基于最近聊天重新整理；已晋升与已厘清的历史保留；未同意先 503 不删一行。
- **工作台状态页签**：待确认 / 已晋升 / 已厘清 / 不算了，分桶管理派生理解。

**工程治理（内部）**
- 迁移 `20260908230000_canonical_governance`（`memories.origin/source_ref`、`derived_insights.resolution`）；`resolveInsight` 与 `promoteInsight` 共用规范化键去重；`rebuildInsights` 同意门先于删除。
- 工作台页 `loadCurrentTab` 单一拉取口径；导入路径（importService）复用 createMemory 唯一校验权威，origin/sourceRef 随候选落库。

### 2026-09-08 工作模式功能桌面 + 3D 衣柜 + 化妆间自定义妆容

**新增（用户可感知）**
- **工作模式「功能桌面」**：工作模式下未选会话时，聊天主区空态直出全部功能入口网格（日记/日程/经期/倒数日/手帐/阅读/自习/工作台/提醒设置/3D 衣柜/化妆间）；选中会话后 ChatHeader 新增「功能」按钮随时唤出同一面板（遮罩层）。切回聊天模式恢复原插画空态。
- **3D 衣柜（`/tools/wardrobe`）**：上传单品照片 → 外部图生 3D → GLB 模型落库存盘，衣柜列表 + `<model-viewer>` 旋转预览 + 下载/删除。外部 3D 服务尚未接入：未配置时上传如实 503（「3D 生成服务还没接好」），不落行也不写文件，绝不做假成功。
- **化妆间（原美颜相机升级改名 `/tools/makeup-room`）**：四滑杆实时美颜不变（照片仍全程本机处理）；新增自定义妆容预设——当前滑杆组合存为命名预设、随时点用、重命名、删除，服务端落库多设备同步（`/api/makeup-presets`）。

**工程治理（内部）**
- Prisma 新增 `makeup_presets` / `wardrobe_items` 两表（迁移 `20260908160000_makeup_wardrobe`，已经真实 PostgreSQL `migrate deploy` 应用并 `migrate diff` 零漂移验证）。
- 衣柜文件存 `data/wardrobe/<userId>/<itemId>/`（tmp+rename 原子写；创建时写盘失败删行回补，不留幽灵行）；图生 3D 接入位 `services/imageTo3dService.js`（`IMAGE_TO_3D_API_URL`/`IMAGE_TO_3D_API_KEY`，未配置抛 503 + `IMAGE_TO_3D_NOT_CONFIGURED`）。
- 数据导出同步扩面：导出包新增 `makeupPresets`（全字段）与 `wardrobeItems`（仅单品元数据；照片/GLB 沿用 v1 二进制资产边界），「全部 14 张表按用户过滤」有测试锁定；导入端只读既有段落，新段落自然忽略。
- `ToolsPage` 的 TOOLBOX/图标/配色提取为 `features/toolbox.js` 共享模块；`<model-viewer>` JSX 类型声明入库。
- 隐私合同同步：3D 衣柜单品照上传服务器并转交外部 3D 服务（隐私政策 §3.5、FAQ Q24、用户手册第四/六节均已如实改写）。
- 测试：API +27 例（预设 CRUD/校验、3D 接入位、衣柜服务真临时目录闭环、两套路由 supertest）；Web +18 例（化妆间预设交互、衣柜页、WorkDesktop、ChatHeader/ChatPage 工作模式分支）并修复 1 个真实 bug（上传须 `api.postForm`，实例默认 JSON Content-Type 会让 multer 解析不到文件——由真机 E2E 抓出）。全量门禁：API 594 通过 / Web 536 通过、typecheck 0、build 通过、覆盖率 94.56/87.89/86.87/94.56；真实端到端（内嵌 PostgreSQL + 真登录）：登录→存妆→改名→删除→刷新持久化、衣柜 503 文案、工作桌面 11 卡全部通过。


---
### 2026-09-07 P1：可携带的记忆——导出/导入与角色恋人红线（迁移窗口）

**新增（用户可感知）**
- 「我的 → 数据与迁移」：**导出我的全部数据**为单个 JSON 文件（人格、角色扮演、显式记忆、全部对话、日记、手帐、日程、倒数日、经期、提醒、阅读、自习），永久免费、不设会员门槛、不需要任何理由；不导出凭据、危机日志与二进制资产（v1 边界）。
- **导入数据**：把别处下线的 AI 姐妹搬回家——上传Amie导出包，或粘贴豆包/千问智能体的人设文本；先解析预览，逐条勾选确认后才落库；角色扮演与人格可一并恢复。云端模型同意状态不随包导入，须主动重新同意（`cloud-primary-v1`）。
- **角色扮演恋人红线**：角色名或设定命中恋人/情侣/伴侣/男女朋友/灵魂伴侣/主人等亲密关系称谓时如实拒绝（400），文案沿用品牌口径「我是你姐妹，不是你对象」；普通非亲密关系（合租室友、同桌）不受影响。导入路径复用同一道闸。

**工程治理（内部）**
- `exportService.buildUserExport` + `GET /api/user/export`（Content-Disposition 附件）；`importService` + `POST /api/user/import/preview|apply`（预览绝不落库，应用只落确认候选）；记忆经 `createMemory` 校验，规范化去重（NFKC+trim+小写）双向生效。
- 日志只记 userId 与各节条目数/导入计数，不记导出或导入内容。
- 测试：export 8 例、importService 12 例、ImportMigration 组件 6 例、红线 3 例；全量门禁：API 530 / 覆盖率 91.64/80.45/92.05/92.61，Web 515 / typecheck 0 / 覆盖率 95.13/88.91/89.32/95.13，全部通过。

### 2026-09-07 云端切割：砍本地模型 + 砍服务器侧执行能力（BREAKING，用户拍板）

**破坏性变更（用户可感知）**
- **聊天切换为云端唯一路径**：目标用户是手机用户，跑不了本地模型。llama.cpp 管理页、实例级模型配置表（`LlmRuntimeConfig`，新增 drop 迁移）、实例管理员中间件、网关本地 provider 全部删除。供应商槽 `GATEWAY_QWEN_*`（供应商中立）成为唯一 provider。
- **同意版本升级 `cloud-primary-v1`**：云端从「可选备用」变成「唯一聊天路径」，数据处理的范围与目的实质变化，旧版同意（`qwen-fallback-v1`）全部自动回到未选择状态，首次进入聊天须经首屏同意门重新授权。同意门不变且更强：每次云端调用前服务端重读同意状态，撤回授权会拦截尚未发出的请求；记忆候选、日记/手帐/阅读/自习短评走同一同意门。
- **虚拟试衣间/虚拟化妆间下线**：云端部署下「本机」语义不再成立，服务器侧生图与多租户共享主机不可兼得；能力注册表只留美颜相机（MediaPipe WASM 全程浏览器内处理，照片真不上传）。
- **错误码收敛**：`LOCAL_LLM_NOT_CONFIGURED` / `LOCAL_LLM_UNAVAILABLE` 删除，改为 `CLOUD_NOT_CONSENTED`（未同意）与 `LLM_UNAVAILABLE`（供应商不可用）；消息来源徽标收敛为「云端模型 / 本地安全模板」。
- **落地页诚实化**：删除全部未经代码兑现的宣称——「四层记忆系统 · 北邮 MemoryOS」「8 种情绪识别 · DPO 微调」「Qwen3-8B 本地部署 + vLLM」「端到端加密」「99.9% 可用性」「24h 在线陪伴」「内测用户证言（白名单仅 2 个手机号）」「Powered by 火山引擎」「18 元/月 + 7 天免费体验」。替换为逐条可验证的真实宣称，并新增「我们不做的事」板块（不做虚拟恋人 / 不抽卡 / 不用对话训练模型）。

**安全移除（多租户云端下的共享服务器执行面）**
- `bash_run`（API 进程用户上下文任意 shell，旁挂 JWT 签名密钥与数据库口令）
- 内置浏览器五件套（`browser_open/read/click/type/close`，Playwright 驱动真实 Chromium）
- 工作模式本机生图（`generate_image`）与 ComfyUI 适配层；API 镜像系统 Chromium 与 compose `shm_size` 同步移除
- SKILL.md 技能加载、JS 插件、MCP 连接（默认均为开启态，含 filesystem MCP 示例）
- `web_search` 保留但换实现：Playwright 抓 Bing → 普通 HTTP 抓 DuckDuckGo（`searchService.js`），不再有浏览器进程与用户可控导航面

**工程治理（内部）**
- 工作模式工具 14→6：日程四件、`calc_convert`、`web_search`（写作/翻译/计划由提示词承担）；聊天模式保持 18 个工具（含 `web_search`）。
- 新增 `modelStatusService`（web，只读 `/llm/status`）与 `searchService`（api）；`llmFeatureService.getLlmStatus` 收敛为 `mode: external_primary` 恒值 + `local: { configured:false, state:'removed' }`。
- `runtime.js` 删除 `LOCAL_LLM_ALLOWED_ORIGINS` 校验；供应商规则改为「内测环境只允许 qwen」。
- 提交在途工作安全网（917 语料文件与 dev.db 停止跟踪，私有副本 917/917 SHA-256 校验通过）后才执行切割，保证 7 个从未提交的切割目标可恢复。
- 全量门禁：API lint 0 errors / 508 测试 / 覆盖率 92.01/80.14/93.21/93；Web lint 0 / typecheck 0 / 506 测试 / build / e2e 34 / 覆盖率 95.25/89.26/89.9/95.25，全部通过。


## [未发布] — 内测准备中

> 目标发布日：2026-10-01
> 实施基线：[`docs/Spec_Amie_v1.0.md`](docs/Spec_Amie_v1.0.md)
> 排期见：[`docs/01-产品/开发计划与路线图.md`](docs/01-产品/开发计划与路线图.md)

### 2026-09-05（续）聊天/工作模式分离、人格扩充与工作模式 skills

**新增（用户可感知）**
- 聊天页顶部「聊天 | 工作」分段开关：会话按模式归属与过滤，工作会话使用无人格的效率助手系统提示（直接、结论先行）；聊天模式行为不变
- 人格从 3 个扩到 6 个：新增元气炸弹（energetic）、知心姐姐（sister）、高冷靠谱（cool），「我的」页即时切换
- 工作模式 skills：日程增查完删（复用既有待办实现）、确定性计算与单位换算（长度/重量/温度）、写作/翻译/润色由工作系统提示词直接承担（不做假保存动作）
- 工作模式可驱动内置 Chromium：打开网页、读取标题/正文摘要/可操作元素编号、点击、输入、关闭；dev 主机默认弹可见窗口，容器内强制无头；`GET /api/work/status` 暴露浏览器可用性，工作模式头部显示「浏览器已就绪/未启用」

**工程治理（内部）**
- `Conversation.mode`（chat|work，默认 chat）落库迁移；agentService 双注册表（CHAT_TOOLS/WORK_TOOLS）+ 模式门，跨模式工具调用回喂「当前模式不支持该操作」；网关 scene 路由复用（work 场景不注入人格提示词），本地模板工作场景统一兜底文案
- 新增 `calcService`（手写 tokenizer + shunting-yard，无第三方依赖）与 `browserService`（playwright-core 懒加载单例、每用户独立 context、10 分钟空闲回收、优雅关闭挂钩）；浏览器日志只记 action/userId/host
- 部署面：API 镜像内置系统 Chromium（PLAYWRIGHT_CHROMIUM_EXECUTABLE_PATH），compose api 服务 `shm_size: 1gb`；`BROWSER_ENABLED` 默认关闭


### 2026-09-06 工作模式 skills 扩充：计划/目标约定与本机生图

**新增（用户可感知）**
- 工作模式新增计划/目标管理：先做结构化拆解（目标→阶段→带日期行动项），用户确认后逐项落日程，问进度时先查日程再答（提示词级，无新表）
- 工作模式新增 `generate_image`：经本机 ComfyUI txt2img 出图（与虚拟房间同一 epicrealism checkpoint），落盘到 `data/work-images/<userId>/`，聊天气泡内 chip 附图渲染缩略图（blob 鉴权拉取，按用户隔离）；ComfyUI 未配置/离线时诚实失败

**工程治理（内部）**
- imageGenService 导出 ComfyUI 适配原语（在线探测/提交/轮询/取图）供 workImageService 复用；`GET /api/work/images/:name` 鉴权 + 文件名形态校验 + 用户目录隔离；toolRuns 持久化新增可选 imageId 字段（Json 列，无迁移）

### 2026-09-05 界面焕新与桌面端

**新增（用户可感知）**
- 桌面端不再是手机模拟器：>640px 视口切换为真桌面布局——左侧导航栏 + 居中宽内容区；聊天页 ≥1024px 有会话列表侧栏（切换/新建/删除会话，删除走确认弹窗）；发现页卡片双列；登录页为环境渐变底上的居中卡片
- 视觉焕新：新增薰衣草 pastel 令牌、衬线展示标题类、悬停抬升与滚动入场动效；落地页按编辑风重做（衬线斜体强调、药丸 CTA、✦ 点缀、细分隔线），支持滚动入场 + hero 鼠标视差 + 平滑锚点滚动（reduced-motion 全禁用）
- 配图全部由本地 ComfyUI 重新生成（落地页 hero/关于区、登录主图、姐妹头像、发现页横幅、聊天空态），统一 pastel 编辑风

**工程治理（内部）**
- PhoneFrame 移除（AppShell 取代）；TabBar 与侧栏共用 APP_TABS 单源；落地页删除未引用的旧资产（hero.png、ark-gen*）
- 宽屏聊天页宽度修正：聊天路由桌面限宽放宽至 1240px，气泡 ≥1024px 放宽至 620px；气泡支持 `**加粗**` 轻量渲染（此前模型输出的 markdown 星号原样漏出）

### 2026-09-05（续）虚拟房间本地生图

**新增（用户可感知）**
- 虚拟化妆间 / 虚拟试衣间从「接入中」占位变为真实功能：选照片 + 挑妆容/单品后，由本机 ComfyUI（epicrealism-inpaint 工作流，SAM3.1 文本分割蒙版缺失时退化为全图 i2i）生成上妆/穿搭预览，可保存；ComfyUI 不在线时仍诚实显示「接入中」（503 `IMAGE_GEN_UNAVAILABLE`）
- 生图提示词由主模型把目录中文描述 + 备注改写为英文提示词；主模型不可用时退化为目录描述直拼的确定性模板

**隐私合同更新（依 local-first-capabilities §2 第 4 款程序）**
- 照片流转变为 浏览器 → 本机 API（multipart ≤8MB，仅 JPEG/PNG/WebP，内存不落盘）→ 本机 ComfyUI；不送任何外部模型/外部生图 API。已同步 Spec §7 增补、隐私政策 §3.5、API 文档第十节、用户手册第五节

**工程治理（内部）**
- `imageGenService` 重写为 ComfyUI 适配层（`/upload/image` → `/prompt` → `/history` 轮询 → `/view`），新增 `IMAGE_GEN_PROVIDER`（默认 `comfy`，`external` 保留旧不可用接缝）；`POST /api/virtual/image-gen/generations` 改 multipart（multer memoryStorage）；API 侧新增 `virtualStudioCatalogs.js` 作为提示词目录单一事实源

### 2026-09-05（续）妆教模块下线

**移除（用户可感知）**
- 妆教模块（`/makeup/`）整体下线：发现页入口、落地页「AI 妆教」板块、`packages/makeup-skill` 包、Compose `makeup` 服务与 Nginx `/makeup/` 挂载、`POST /api/llm/explain` 及 API 侧妆容目录镜像与漂移守卫全部移除；`GET /api/llm/status` 保留不变
- 文档同步：Spec §7 改写为「虚拟房间合同」、隐私政策移除妆教条款（§3.5 虚拟房间流转说明保留）、用户手册删除妆教章节并重编号、FAQ 删除 5 条妆教问答并重编号为 31 条、部署手册删除受控资产注入步骤

**工程治理（内部）**
- `llmService.generateExplanationWithModel` 保留（虚拟房间生图提示词改写复用），仅注释更新；`llmFeatureService` 收敛为纯模型状态服务

### 2026-09-05（续）待办升级为日程

**新增（用户可感知）**
- 工具箱「待办清单」升级为「日程」：每条可带日期（yyyy-MM-dd）与时间（HH:mm，需先有日期）；主视图改为按天时间线（已过期红色置顶 → 今天 → 明天 → 之后逐日 → 无日期 → 已完成），组内按时间排序；聊天里「帮我记个日程：周六下午三点复诊」会带日期时间落库
- 说明：内测没有通知投递通道，「提醒」由时间线的已过期/今天高亮承载，不做假的推送开关

**工程治理（内部）**
- `Todo` 新增 `dueTime` 列（迁移 `20260905144515_add_todo_due_time`）；`dueDate` 收紧为 `yyyy-MM-dd` 契约（同日记 UTC 零点存储）；`add_todo`/`list_todos` 智能体工具支持 `dueTime`；路由 `/tools/todo` 保持不变

### 2026-09-04（续）日记与手帐

**新增（用户可感知）**
- 日记（发现 → 日记）：按天记录心情与正文（5 种心情、月历心情点、可改历史）；「让姐妹看看」生成人格化 AI 回应（幂等复用、编辑后自动作废可重生成；人格 + 心情 + 脱敏正文，同意门与聊天一致）
- 手帐打卡（发现 → 手帐打卡）：自定义习惯（6 图标、上限 12）、按天打卡、连续天数与 30 天点阵；「姐妹说两句」按聚合打卡数据生成鼓励（只送习惯名与计数，不送正文）
- 智能体新增 4 个工具：`add_diary`、`diary_status`、`check_habit`、`habit_status`——聊天里可以直接说「帮我记个日记」「帮我打卡喝水」「我今天写日记了吗」；`check_habit` 幂等置位，不会重复翻转

### 2026-09-04（续）外部主用部署模式

**新增（用户可感知）**
- 新增部署模式 `EXTERNAL_CHAT_PRIMARY`：外部供应商槽（任意 OpenAI 兼容端点）可作为聊天/解释主力模型，llama.cpp 变为可选回退；不再要求本地模型必须配置。同意门保持不变——未明示同意前外部模型调用次数为零，同意文案与消息徽标按模式区分「云端模型/云端备用」，「我的 → 云端模型」与 `/api/llm/status`（`mode: external_primary`）同步暴露

**修复（用户可感知）**
- 流式工具调用前缀门不再把纯空白首分片误判为自然语言（推理模型先发换行/空格时，工具 JSON 会原文泄漏到对话里）
- 工具回路级去重：模型重复调用同一操作时只执行一次并回喂引导（此前会创建重复待办）；成功反馈自带「已完成，直接回复」提示
- 记忆建议的敏感排除修复 NFKC 误伤：含全角标点的正常中文候选不再被误判为敏感内容；抽取与妆教解释的 token 预算按推理模型上调（400→2000 / 120→1500，避免思考预算吃光导致空回复）

### 2026-09-04 安全流式聊天与按需记忆建议

**新增（用户可感知）**
- 聊天改为分句流式输出：回复逐句出现；每句通过累计安全检查后才下发，命中红线时整段替换为本地安全模板；中途断线/失败不留下半句、不落库，输入保留可重试（网关 → API SSE → Web 全链路，Nginx 对该路径关闭缓冲）
- 聊天内最新回复新增「帮我记住」入口：本地模型从你说的那句话临时生成最多 2 条记忆候选，逐条可编辑、保存或忽略；未经确认绝不落库；危机/联系方式/证件号/精确位置/医疗内容不生成候选

- 聊天具备智能体工具调用（参考 pi-agent-core 的 agent loop，按本产品安全模型收敛）：对她说「帮我记个待办/倒数日/经期」「提醒我…」「我还有几天到…」时，她会在本地模型回路中直接操作你的产品内工具（待办/倒数日/经期/提醒，最多 3 轮），回复下方显示「✓ 已添加待办」等动作标签；不执行任意代码，记忆仍只能经「帮我记住」确认

**工程治理（内部）**
- 修复 llm-gateway 对 OpenAI 兼容地址重复拼接 `/v1` 的缺陷（`baseUrl` 按 OpenAI 约定已含 `/v1`，如 llama.cpp 与 DashScope；此前任何真实上游的非流式/流式调用都会 404）
- llm-gateway 新增可取消的原生 SSE 流方法（11 个 node:test 直接回归），授权回退只发生在首个内容块之前
- Web 鉴权刷新逻辑单源化（`refreshAccessToken`），流式 401 只刷新重试一次；迟到流在会话切换后一律丢弃
- `--cs-text-muted` 由 #8A7B83 加深为 #7A666E：辅助文字在全部表面达到 4.5:1 以上对比度，axe serious 门禁恢复通过
- API 文档新增 SSE 契约与记忆建议端点；用户手册/FAQ 同步流式与「帮我记住」行为

### 2026-09-03 前端界面优化与诚实化修复

**修复（用户可感知）**
- 设置页移除无效果开关（主动关怀消息）、死按钮（清空所有记忆/一键清空对话记录/用户协议）与宣称「7天内彻底删除」的假注销；只保留 3 个真实服务端提醒开关
- 会员中心接入路由（我的 → 会员中心）：删除伪支付按钮与假开通流程，改为静态展示与「内测期间全部功能免费开放，会员体系暂未上线」明示
- 记忆页「清空全部」改为应用内确认弹窗（危险操作默认焦点在取消）；待办/倒数日/经期三页新增加载与失败重试态
- 发现页内部能力卡改为 SPA 跳转（不再整页刷新）

**工程治理（内部）**
- 落地共享 UI 原语（Button/Card/Modal/ConfirmDialog/EmptyState/Spinner），旧页面完成迁移
- 原生色板（`bg-white`/`text-white`/`red-*`/`gray-*`）全仓清零，globals.css 遗留别名层与无引用工具类删除，tailwind 映射收敛
### 2026-09-01 五轴工程治理

**修复（用户可感知）**
- 长会话重新进入只显示最旧 50 条 → 默认加载最新消息并按时间升序渲染（分页方向修正 + 排序加 id 次序键）
- 经期日历与「距离下次还有 N 天」按本地日历日计算，修复 UTC+8 下整体偏移 1 天
- 每日使用时长统计改用北京时区日界（原 UTC 日界导致每天早 8 点被清零）；客户端用量提醒优先消费服务端口径
- 危机上报记录统一不再留存触发原文（triggerMsg 不落库），聊天与合规两条写入路径合一
- 静态照片美颜滑杆拖动不再叠加数十条人脸检测推理（同图检测缓存 + 处理串行化 + 同帧合并），消除卡顿来源

**工程治理（内部）**
- 文档：两份代码审查文档重写至 monorepo 现状；6 份文档横幅与索引档位对齐；8 份历史架构文档物理移入 `docs/09-参考/历史归档/`
- 架构：抽出 `authService`/`crisisService`，路由不再直写数据库；人格 ID 单源化（网关 personas.js 为权威）；妆容目录与肤色枚举两侧由漂移守卫测试锁定一致
- 性能：美颜滤镜每帧约 16MB 短生命周期分配降至稳态近零（模块级乒乓缓冲）；形变渲染复用目标画布；危机检测关键词改为模块加载时预归一化
- 规范：三包 ESLint 新增 complexity(15)/max-depth(4) 警告与 no-await-in-loop 错误（测试文件豁免）；记忆列表分页改并行拉取
- 测试：全仓 1615 个测试通过；apps/api、apps/web、makeup-skill 行覆盖 95–97%、分支 87–91%，80% 阈值门禁全过；llm-gateway 直接测试随流式工作包 task-1 补齐（已知问题 #8）

### 2026-08-31 仓库收敛与品牌统一

- **单仓库单品牌**：cyber-sister 成为唯一活跃仓库与唯一品牌「Amie」。原 `AI妆教/` 仓库归档至 `../_archive/AI妆教/`（其最终提交 `f2dc25b` 早已 subtree 并入本仓库，归档前确认工作区干净、无独有内容）
- **落地页迁入**：主落地页从 `电子闺蜜/landing/` 迁至 `apps/web/public/landing/`（`/landing/` 路径直接可访问），并新增「AI 妆教」板块承接对外叙事，导航栏加入口
- **品牌统一**：妆教模块不再使用独立品牌「妆语」，统一为「Amie AI 妆教」（功能名「妆教」）；独立品牌站 `packages/makeup-skill/brandsite/` 下线（内容可从 git 历史找回）；11 份历史文档加品牌并入横幅
- **补迁资产**：六张妆容设计参考图迁入 `packages/makeup-skill/design-reference/looks/`（未接入运行时）
- **安全网**：`.gitignore` 忽略 `mp-models/` 注入产物（36MB MediaPipe 二进制不入库）；移除指向已归档仓库的危险远端 `ai-makeup`
- **灾后重建**：`packages/` 目录遭外部因素整体删除。`makeup-skill` 155 个文件从 git 完整恢复；`llm-gateway` 与 `design-tokens`（此前从未提交）按消费方契约重建并全部通过验证；模型注入源 `.assets/` 从幸存的 `apps/web/public/mp-models/` 恢复，7 个文件 SHA-256 校验全部通过

### 已就绪（待随首个内测版发布）

依据 2026-08-31 代码核查，以下能力已实现，等待走完发布验收门（Spec §9）：

**账号与身份**
- 白名单手机号 + 固定验证码登录，非白名单与错误码统一返回 401，不泄漏白名单状态
- access token 鉴权 + refresh token 轮换与 logout 撤销（HttpOnly / Secure cookie，服务端只存 hash）

**聊天与人格**
- llama.cpp 本地优先的分句安全流式聊天（SSE；每句累计安全检查后下发，中途失败不落半条）
- 三种人格 `toxic` / `gentle` / `rational` 即时切换，切换后下一条消息生效
- `qwen-fallback-v1` 可选云端备用：同意 / 拒绝 / 撤回三态，由服务端门控，未同意时 Qwen 调用次数为零
- 送入模型的数据脱敏，业务消息最多 20 条，记忆最多注入 5 条

**记忆**
- 显式记忆 CRUD：`semantic` / `episodic` / `procedural` 三类，带 importance 与 tags
- 记忆归属校验，支持清空；系统不自动提取、不由模型推断
- 按需记忆建议：聊天内「帮我记住」触发，本地模型生成最多 2 条候选，用户编辑确认后才经既有创建接口落库

**安全与合规**
- 危机检测先于同意检查与模型调用；中高风险输入事务内阻断并写入 `CrisisLog`，模型调用次数为零
- 安全输出过滤，命中红线（威胁、拱火、浪漫化伤害、冒充真人、替用户决定重大事项）替换为安全模板
- 日志最小化：只记 request ID、scene、provider/model、尝试次数、延迟、结果

**工具箱**
- 待办、倒数日、经期记录、提醒设置
- 天气端点保持 409 关闭（内测不提供）

**运维**
- 单机 Docker Compose：Nginx(TLS) + Web + API + PostgreSQL + 一次性 migration job
- `/api/health/live` 与 `/api/health/ready` 分离；本地模型未加载时应用仍 ready
- 不可变镜像 tag，纯初始迁移（禁 `db push`）

### 已知限制（内测期不会修复）

- 虚拟试衣间 / 虚拟化妆间：依赖本机 ComfyUI 在线；ComfyUI 关闭时生图不可用（入口诚实显示"接入中"）
- 会员中心：仅页面展示，无真实支付
- 无自动记忆提取，无向量检索（记忆建议为按需候选，需用户确认后才落库）
- 无语音 / 图片生成，无真实短信，无账号删除
- 工具箱范围的文档与实现冲突已裁定（C1）：工具箱纳入内测，见 `docs/README.md` 已知文档问题表 #1 与开发计划第 1.3 节

---

## 仓库历史

### 2026-08-28 · monorepo 合并

- `f4846d8` P1: monorepo 骨架 + 工具目录与数据源迁入
- `1236570` 从独立仓库合入 `packages/makeup-skill`（妆教模块）
- `3bd7928` P1: 迁入Amie前后端、文档与设计资产

### 2026-06 ~ 2026-07 · 妆教模块（合入前历史，来自 makeup-skill 仓库）

- `f2dc25b` feat(P0): D6 品牌站独立部署决策 + D9 问卷/落地页
- `23e7638` feat(P1): D10 术语括注 + D11 收藏回看 + D12 场景标签
- `4ed8e78` feat(analytics): P0 埋点链路 + 治理收尾
- `4995113` refactor: 剥离超范围功能，回归 MVP 核心 + 架构重构 + 安全加固
- `731cb91` feat(security): CSRF 双重提交 Cookie 防护
- `48c48cd` feat(security): 严格 schema 验证 + mass assignment 防护
- `4276294` feat(security): 严格 CSP / Permissions-Policy / Referrer-Policy / HSTS
- 以及一批无障碍、触控目标、性能与交互细节提交（44px 触控、键盘导航、reduced-motion、焦点陷阱、空闲预取、埋点缓存等）

---

## 版本命名约定

内测期间采用 `v0.x.y-internal.N`：

- 首个内测基线：`v0.1.0-internal.1`
- 后续内测迭代递增 `N`
- 修复与小幅调整递增 `y`
- 能力范围变化递增 `x`

发布时必须同时更新：

- 本文件
- Release Notes（模板见 `docs/09-参考/发布说明模板.md`）
- `docs/01-产品/开发计划与路线图.md` 的进度状态
