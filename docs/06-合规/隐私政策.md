# 隐私政策

> ⚠️ **本文为草案 V1.0，尚未经法务审核，不得对外发布。**
>
> - 创建日期：2026-08-31
> - 依据：[`docs/Spec_赛博姐妹_v1.0.md`](../Spec_赛博姐妹_v1.0.md) §3（同意门）、§4（脱敏规则）、§7（图像能力合同）、§8（日志规范）
> - 生效范围：内测版（VPN / 可信私网，白名单成年测试者）
> - 待补：运营主体名称、联系邮箱、数据留存期限的具体数值、跨境（如适用）
>
> 发布前必须由法务/合规负责人审核并签字，审核记录附于文末。

---

## 引言

赛博姐妹（"我们"）是一款面向中国成年用户的 AI 陪伴产品。我们深知，你愿意把心里话说给一个 AI 听，本身就是一种信任。这份政策用尽量直白的话说明：我们收集什么、怎么用、怎么保护、以及你能怎么管。

**一句话版本**：聊天由经批准的云端模型提供，送给云端的内容会脱敏，不获你同意一个字也不发；日志不记聊天内容；你的自拍照从不上传；你随时可以删掉我们记住的一切。

---

## 一、我们收集哪些信息

### 1.1 你主动提供的

| 信息 | 用途 | 是否必须 |
|------|------|----------|
| 手机号 | 登录验证、账号识别 | 是（登录必需） |
| 聊天内容 | 生成回复、展示历史 | 是（服务核心） |
| 记忆内容 | 让你不用重复说 | 否（你自主创建） |
| 工具箱数据（待办、倒数日、经期、提醒） | 提供工具功能 | 否 |
| 人格偏好 | 决定她的说话方式 | 否（有默认值） |

> 「帮我记住」的记忆建议候选由云端模型临时生成（与你同意的聊天同一同意门、同一脱敏规则），只有在你确认后才会成为上表中的记忆内容；未确认前不落库。

### 1.2 我们自动记录的

| 信息 | 用途 | 说明 |
|------|------|------|
| 登录与刷新令牌（服务端只存哈希） | 保持登录、安全轮换 | 令牌本身不落库 |
| 请求日志 | 排查故障、保障安全 | **只含** request ID、场景、模型名、重试次数、耗时、结果 |
| 功能使用事件 | 了解哪些功能被用 | 不含聊天内容 |

### 1.3 明确不收集的

- ❌ **你的自拍原图**：美颜相机的照片**只在你这台设备的浏览器内处理**（MediaPipe WASM），不送往任何服务
- ❌ **通讯录、位置、相册、麦克风**：不申请、不采集
- ❌ **设备指纹、广告标识符**：不做用户追踪，不接第三方广告 SDK

---

## 二、我们如何使用信息

只用在这几件事上，**不用于广告、不出售给第三方**：

1. **提供服务**：生成回复、保存历史、管理工具数据
2. **保障安全**：危机识别、异常登录检测、防滥用
3. **排查故障**：通过请求日志定位问题
4. **改进产品**：基于聚合的、去标识化的使用统计

**我们不会**：

- 用你的聊天内容训练公开模型（内测期间）
- 把你的数据卖给任何第三方
- 向你推送基于画像的定向广告

---

## 三、信息如何流转

### 3.1 云端唯一路径

2026-09-07 起，聊天与解释（含「让姐妹看看」日记回应、「姐妹说两句」手帐鼓励、阅读/自习短评、记忆建议候选）只有**云端模型**一条路径：本地模型面已删除。你的消息经我们的服务器脱敏后转发给经批准的模型供应商处理。

### 3.2 云端模型（必须先经你同意）

- **必须你主动同意**才可能启用，默认关闭
- **未同意时云端调用次数为零**：聊天与上述短评类能力都不可用，进入聊天会看到首屏同意门
- **随时可撤回**，撤回后立即生效；服务端在每次调用前都会重新读取你的当前同意状态，因此你在某次请求等待期间撤回，也会拦住尚未发出的请求
- 同意版本为 `cloud-primary-v1`：2026-09-07 云端切割使数据处理的范围与目的发生实质变化，旧版同意自动回到未选择状态，须重新授权

### 3.3 送给模型前会做什么处理

无论走哪条路径，送入任何模型的内容都遵守：

- 业务消息最多 **20 条**（19 条历史 + 当前输入），按时间旧到新
- 只含角色和**脱敏后**的内容
- 记忆最多注入 **5 条**，且必须是与当前输入有明确重合的
- **手机号、邮箱、证件号等确定性脱敏**
- 不发送用户 ID、手机号字段、时间戳、图片、日志或无关记忆
- 日记回应（「让姐妹看看」）：仅该篇日记正文经脱敏后送入模型；手帐鼓励（「姐妹说两句」）：只送习惯名、连续天数与今日完成计数，不送任何正文。两者都沿用 §3.2 的同意门

### 3.4 日志

日志只记录：request ID、场景、模型/供应商、尝试次数、延迟、结果。

**不记录**：提示词正文、聊天内容、记忆内容、凭据、图片。

### 3.5 美颜相机的照片

使用美颜相机（`/tools/beauty-camera`）拍照或选照片时，照片**全程只在你这台设备的浏览器内处理**（MediaPipe WASM 实时磨皮/美白/瘦脸/大眼）：不发送我们的服务器，不发送任何外部模型或外部生图服务。虚拟化妆间/虚拟试衣间（本机 ComfyUI 生图）已于 2026-09-07 移除下线。

---

## 四、我们如何保护信息

| 措施 | 说明 |
|------|------|
| **同意门** | 未获你同意前不向云端模型发送任何内容；每次调用前服务端重读同意状态 |
| **传输加密** | HTTPS/TLS 全程加密 |
| **令牌安全** | refresh token 仅存于 Secure + HttpOnly Cookie，服务端只保存哈希，每次使用轮换 |
| **脱敏** | 送模型前对个人标识信息做确定性脱敏 |
| **日志最小化** | 不落库聊天正文 |
| **访问控制** | 密钥、白名单、模型地址等配置不进代码仓库，通过外部只读文件挂载 |
| **数据库** | 只通过受控迁移变更，发布前备份 |

> 我们不会声称"绝对安全"——没有任何系统能做到。但上面这些是我们实际做了的，不是宣传话术。

---

## 五、你的权利

你可以随时：

| 权利 | 怎么做 |
|------|--------|
| **查看** | 「我的」→「记忆」查看她记住的全部内容 |
| **修改** | 记忆可逐条编辑 |
| **删除单条** | 记忆列表中删除 |
| **删除全部** | 记忆页一键清空 |
| **导出** | ⚠️ 内测版暂不支持，已列入后置计划 |
| **删除账号** | ⚠️ 内测版暂不支持（后置项），当前可清空记忆 |

**需要帮助**：如需我们协助处理你的数据，通过下方联系方式提出。

---

## 六、未成年人保护

- 本服务**面向成年人**，内测为白名单邀请制，仅面向成年测试者
- 我们不面向未满 18 周岁的用户提供服务
- 如发现未成年人使用，我们将停止服务并删除相关数据
- 监护人如认为被监护人使用了本服务，请联系我们处理

> 未成年人保护的具体技术措施见 [`docs/01-产品/开发计划与路线图.md`](../01-产品/开发计划与路线图.md) W3-3。

---

## 七、关于 AI 的特别告知

请你始终记住：

- **她是 AI，不是真人**。每个界面都有身份标识
- 她的回复由模型生成，**可能不准确、可能编造信息**，重要事项请自行核实
- **她不做心理咨询或医疗诊断**，不能替代专业人士
- **她不会替你做重大决定**

当她说的内容涉及伤害自己或他人时，我们会中断 AI 回复并提供求助资源。这些资源经人工核验，但不保证可用性——**遇到紧急情况请直接联系当地紧急服务或专业机构**。

---

## 八、信息留存

| 数据 | 留存策略 |
|------|----------|
| 聊天记录 | 账号存续期间保留，用于展示历史（**具体期限待法务确定**） |
| 记忆 | 直到你主动删除 |
| 工具箱数据 | 直到你主动删除 |
| 请求日志 | **保留期待定**（待法务确定，建议不超过 30 天） |
| 危机事件记录 | 用于安全审计，**保留期待定** |

> ⚠️ 上表具体数值需法务/合规负责人确定后填入。

---

## 九、政策更新

如本政策有实质变更，我们会在产品内提示。继续使用视为接受更新后的政策；如你不同意，可停止使用并删除数据。

---

## 十、联系我们

- **运营主体**：（待填写）
- **隐私相关联系**：（待填写邮箱）
- **数据主体请求**：（待填写渠道）

---

## 附：审核记录

| 版本 | 日期 | 审核人 | 角色 | 结论 | 签字 |
|------|------|--------|------|------|------|
| V1.0（草案） | 2026-08-31 | — | — | ⛔ 未经审核，不得发布 | — |

> **发布前置条件**：法务/合规负责人审核通过并在此表签字。未经签字的隐私政策不得对外展示或引用。
