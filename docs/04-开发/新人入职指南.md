# 新人入职指南

> **适用**：新加入的开发者 / 测试 / 产品
> **创建日期**：2026-08-31
> **配套**：环境搭建细节见 [`CONTRIBUTING.md`](../../CONTRIBUTING.md)，本文侧重**学习路径与上下文**

---

## 零、先建立正确的心智模型

在碰代码之前，请先接受三件事——它们会解释这个项目里很多"看起来奇怪"的设计：

**1. 这是一个"有边界"的情感产品，不是通用助手。**

我们刻意**不做**虚拟恋人、不做心理咨询、不做通用工具。所有边界都是为了合规与用户安全。当你觉得"这里限制太多了"的时候，先问一句：这个限制是在保护用户，还是在保护我们的合规底线？

**2. 文档比代码更需要维护，但文档经常是错的。**

这个仓库有大量文档，但其中不少是**历史归档方案**（微服务、K8s、Redis、向量库等），写得详实但**不代表当前实现**。读到任何架构文档，先看顶部有没有"历史/未来方案归档"警告。

**3. "没做完"必须诚实标注。**

生图没接好就显示"接入中"，天气没数据就 409 关闭，会员没支付就明说。这是我们产品的一部分，不是需要掩盖的缺陷。

---

## 一、第一天：跑起来

### 目标

能在本地登录，发出第一条消息。

### 步骤

1. **读三份文档**（约 40 分钟）
   - [`README.md`](../../README.md) — 内测范围、仓库结构、完整启动命令
   - [`docs/Spec_Amie_v1.0.md`](../Spec_Amie_v1.0.md) — **实施基线合同**，最重要
   - [`CONTRIBUTING.md`](../../CONTRIBUTING.md) — 环境搭建与提交规范

2. **搭环境**（约 1 小时，按 CONTRIBUTING 步骤）
   - Node.js 20+ / pnpm 11.5.1 / Docker
   - `pnpm install --frozen-lockfile`
   - `docker compose -f compose.dev.yaml up -d`
   - 配置 `../.local-secrets/` 下的密钥文件

3. **跑起来并登录**
   - 启动 API 与 Web
   - 用白名单手机号 + 固定验证码登录

4. **预期会遇到的"不是 bug"**
   - 未配置 llama.cpp → 聊天返回 `LOCAL_LLM_NOT_CONFIGURED`（正常）
   - 未注入 MediaPipe 资产 → `pnpm build` 失败（设计如此）
   - 天气不可用（内测 409）

> 第一天跑不起来很正常，环境问题卡住直接问，别自己耗。

### 自检

- [ ] 能登录并看到聊天界面
- [ ] 发一条消息，看到返回（或看到明确的 `LOCAL_LLM_NOT_CONFIGURED`）
- [ ] 能打开工具箱、记忆页
- [ ] 知道去哪里提 Issue

---

## 二、第一周：读懂它

### 2.1 代码地图

```
apps/web/              React + Vite 前端
  src/pages/           页面（Login / Chat / Tools / Period / Countdown / Todo / Diary / Handbook / Reading / Study / Workspace / Memories / Profile / Settings / Membership / MakeupRoom / Wardrobe）
  src/components/      组件（chat / layout / beauty / ui / work）
  src/features/        领域逻辑（personas 人格、beauty 美颜引擎、capabilities 能力注册表、toolbox 工具箱目录）
  src/stores/          Zustand 状态（auth / chat / compliance / tools）

apps/api/              Express + Prisma API
  src/routes/          路由（auth / chat / user / memories / tools / compliance / llm / virtualStudio / localLlmAdmin）
  src/services/        业务（chatService 等）
  src/middleware/      中间件（auth 等）
  prisma/              数据库 schema 与迁移

packages/
  llm-gateway/         模型网关（本地优先路由）
  makeup-skill/        妆教模块
  design-tokens/       共享设计令牌
```

### 2.2 必读文档（按优先级）

| 顺序 | 文档 | 为什么 |
|------|------|--------|
| 1 | [`Spec_Amie_v1.0.md`](../Spec_Amie_v1.0.md) | 唯一权威合同，范围与规则都在这 |
| 2 | [`01-产品/开发计划与路线图.md`](../01-产品/开发计划与路线图.md) | 当前在做什么、已知冲突 |
| 3 | [`04-开发/API文档.md`](API文档.md) | 接口全貌 |
| 4 | [`06-合规/内容审核策略.md`](../06-合规/内容审核策略.md) | **红线，必读** |
| 5 | [`07-用户/用户使用手册.md`](../07-用户/用户使用手册.md) | 从用户视角理解产品 |
| 6 | [`代码审查标准与流程_V1.0.md`](../代码审查标准与流程_V1.0.md) | 提交前的自检 |

### 2.3 ⚠️ 这些文档不要照着做

`docs/architecture/` 下原有 8 份 🚫 历史归档文档已于 2026-09-01 物理移入 `docs/09-参考/历史归档/`（下表前 7 行）。它们与 `docs/` 其他带归档/参考横幅的文档一样，描述的是**未来可能或已放弃的架构**（微服务、API 网关、Redis、K8s、向量数据库、性能优化等），**不是当前实现**。

| 文档 | 状态 |
|------|------|
| `API网关与服务治理.md` | 🚫 历史归档 |
| `后端架构重构方案.md` | 🚫 历史归档 |
| `安全防护方案.md` | 🚫 历史归档 |
| `性能优化方案.md` | 🚫 历史归档 |
| `数据库架构设计.md` | 🚫 历史归档 |
| `实现总结.md` | 🚫 历史归档 |
| `架构设计总结.md` / `总结与实施路线图.md` | 🚫 历史归档 |
| `测试与质量保证方案.md` | ⚠️ 参考（非当前基线） |
| `监控与告警方案.md` | ⚠️ 参考 |
| `部署与运维方案.md` | ⚠️ 参考，当前以 `deployment/internal-runbook.md` 为准 |
| `local-first-capabilities.md` | ✅ 当前有效 |

**想做架构决策，以 Spec + `local-first-capabilities.md` 为准。**

### 2.4 理解核心链路

**聊天链路**（最重要，先搞懂这条）：

```
用户输入
  ↓
危机检测（最先！命中则阻断，模型调用为零）
  ↓
同意状态检查（决定是否允许 Qwen 备用）
  ↓
llm-gateway 本地优先路由（llama.cpp → 可选 Qwen → 本地模板）
  ↓
输出安全过滤（命中红线替换为安全模板）
  ↓
持久化用户消息 + AI 消息
```

**关键约束**（背下来）：

- 危机检测**先于**模型调用，命中时**模型调用次数为零**
- 送入模型：业务消息 ≤20 条，记忆 ≤5 条，全部**脱敏**
- 日志**不含**聊天正文、记忆、凭据、图片
- 模型失败 → 该次消息**不持久化**，前端保留输入

### 2.5 第一周任务建议

- 领一个 `good first issue` 或文档类小任务
- 提交 1–2 个 PR，走完整流程（lint / typecheck / test / build / review）
- 在 PR 中实践检查清单

---

## 三、第一个月：能独立负责

### 3.1 能力提升清单

- [ ] 能独立排查登录、聊天、记忆的常见问题
- [ ] 理解三种人格的实现（`features/personas.js`）与提示词约束
- [ ] 理解妆教模块的本地分析流程（`packages/makeup-skill`）
- [ ] 能读懂 `chatService` 的完整逻辑
- [ ] 熟悉发布流程与验收门（[`06-合规/合规审查清单.md`](../06-合规/合规审查清单.md)）
- [ ] 参与过至少一次 Code Review（作为 reviewer）
- [ ] 独立负责并完成一个 P1 需求

### 3.2 常见踩坑

| 坑 | 现象 | 解决 |
|----|------|------|
| **Windows EPERM** | 跑 `pnpm test` 报 Prisma 引擎文件占用 | 先停掉 dev API（`node --watch` 占用文件） |
| **构建失败** | MediaPipe/WASM 资产缺失或 SHA-256 不匹配 | 按设计如此，需运维注入受控资产 |
| **Docker 拉不动** | Docker Hub 不可达 | 用镜像代理拉取后打标签（命令见 README） |
| **误信归档文档** | 按微服务方案实施 | 检查文档顶部警告，以 Spec 为准 |
| **数据库改错** | 用了 `db push` | 只用 `prisma migrate deploy`，`db push` 被明令禁止 |
| **日志泄漏** | 调试时打了聊天正文 | 上线前必须清理，违反 Spec §8 |

---

## 四、项目特有的红线（再次强调）

| 红线 | 违反后果 |
|------|----------|
| AI 身份不可被人格或用户提示覆盖 | 合规风险 |
| 危机检测必须最先，命中时模型调用为零 | **阻塞发布** |
| 未书面批准的危机文案不得进仓库 | **阻塞发布** |
| 自拍/视频帧永不上传 | 品牌信任崩塌 |
| 送模型的数据必须脱敏，≤20 条消息 / ≤5 条记忆 | 隐私风险 |
| 日志不记聊天正文、记忆、凭据、图片 | 隐私风险 |
| 密钥、白名单不进仓库 | 安全事故 |
| 数据库只用 `migrate deploy` | 数据事故 |
| 不弱化安全红线、不引入情感诱导付费 | 合规风险 |

详细规则见 [`06-合规/内容审核策略.md`](../06-合规/内容审核策略.md)。

---

## 五、去哪里找答案

| 问题类型 | 去哪里 |
|----------|--------|
| 文档全貌 | [`docs/README.md`](../README.md)（文档索引） |
| 环境 / 部署 | [`CONTRIBUTING.md`](../../CONTRIBUTING.md) · [`deployment/internal-runbook.md`](../deployment/internal-runbook.md) |
| 这个该不该做 | [`Spec_Amie_v1.0.md`](../Spec_Amie_v1.0.md) §1 |
| 当前排期 | [`01-产品/开发计划与路线图.md`](../01-产品/开发计划与路线图.md) |
| 接口细节 | [`04-开发/API文档.md`](API文档.md) |
| 用户怎么用 | [`07-用户/用户使用手册.md`](../07-用户/用户使用手册.md) |
| 合规红线 | [`06-合规/内容审核策略.md`](../06-合规/内容审核策略.md) |
| 品牌语气 | [`02-设计/品牌规范.md`](../02-设计/品牌规范.md) |
| 不确定 | 提 Issue，标签 `status: needs-triage` |

---

## 六、给新人的一句话

这个项目最难的不是技术，是**克制**——克制地不做恋人、克制地不承诺永远、克制地不为"效果"牺牲诚实。

如果你认同这一点，你会做得很开心。
