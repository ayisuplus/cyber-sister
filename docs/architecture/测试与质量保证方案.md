# 赛博姐妹测试与质量保证方案

**文档版本**：V1.0  
**设计日期**：2026年7月13日  
**设计目标**：全面覆盖、自动化、持续集成、质量可控

---

## 一、测试策略

### 1.1 测试金字塔

```
┌─────────────────────────────────────────────────────────────────┐
│                        测试金字塔                               │
│  ┌─────────────────────────────────────────────────────────┐   │
│  │                    端到端测试 (E2E)                      │   │
│  │                      10%                                │   │
│  └─────────────────────────────────────────────────────────┘   │
│  ┌─────────────────────────────────────────────────────────┐   │
│  │                    集成测试                              │   │
│  │                      20%                                │   │
│  └─────────────────────────────────────────────────────────┘   │
│  ┌─────────────────────────────────────────────────────────┐   │
│  │                    单元测试                              │   │
│  │                      70%                                │   │
│  └─────────────────────────────────────────────────────────┘   │
└─────────────────────────────────────────────────────────────────┘
```

### 1.2 测试类型

| 测试类型 | 覆盖率目标 | 执行频率 | 工具 |
|----------|------------|----------|------|
| **单元测试** | ≥80% | 每次提交 | Jest/Vitest |
| **集成测试** | ≥70% | 每次合并 | Supertest |
| **端到端测试** | 核心流程100% | 每日构建 | Playwright |
| **性能测试** | P95<200ms | 每周 | k6 |
| **安全测试** | 0高危漏洞 | 每月 | OWASP ZAP |
| **代码审查** | 100%覆盖 | 每次PR | ESLint/SonarQube |

---

## 二、单元测试

### 2.1 测试配置

```javascript
// jest.config.js
module.exports = {
  // 测试环境
  testEnvironment: 'node',
  
  // 测试文件匹配
  testMatch: [
    '**/__tests__/**/*.js',
    '**/*.test.js',
    '**/*.spec.js'
  ],
  
  // 覆盖率配置
  collectCoverage: true,
  coverageDirectory: 'coverage',
  coverageReporters: ['text', 'lcov', 'html'],
  coverageThreshold: {
    global: {
      branches: 80,
      functions: 80,
      lines: 80,
      statements: 80
    }
  },
  
  // 模块路径
  moduleNameMapper: {
    '^@/(.*)$': '<rootDir>/src/$1'
  },
  
  // 设置文件
  setupFilesAfterEnv: ['<rootDir>/tests/setup.js'],
  
  // 转换配置
  transform: {
    '^.+\\.js$': 'babel-jest'
  },
  
  // 忽略文件
  testPathIgnorePatterns: ['/node_modules/', '/dist/']
};
```

### 2.2 测试工具

```javascript
// tests/setup.js
const { PrismaClient } = require('@prisma/client');
const Redis = require('ioredis');

// 测试数据库
const prisma = new PrismaClient({
  datasources: {
    db: {
      url: process.env.TEST_DATABASE_URL
    }
  }
});

// 测试Redis
const redis = new Redis(process.env.TEST_REDIS_URL);

// 全局设置
beforeAll(async () => {
  // 清理测试数据
  await prisma.user.deleteMany();
  await redis.flushdb();
});

// 每个测试后清理
afterEach(async () => {
  // 清理测试数据
});

// 全局清理
afterAll(async () => {
  await prisma.$disconnect();
  await redis.disconnect();
});

// 导出测试工具
module.exports = {
  prisma,
  redis
};
```

### 2.3 服务层测试

```javascript
// tests/services/userService.test.js
const { prisma } = require('../setup');
const UserService = require('../../src/services/userService');

describe('UserService', () => {
  let userService;

  beforeEach(() => {
    userService = new UserService();
  });

  describe('createUser', () => {
    it('should create a new user', async () => {
      const userData = {
        phone: '13800138000',
        nickname: '测试用户'
      };

      const user = await userService.createUser(userData);

      expect(user).toBeDefined();
      expect(user.phone).toBe(userData.phone);
      expect(user.nickname).toBe(userData.nickname);
      expect(user.id).toBeDefined();
      expect(user.created_at).toBeDefined();
    });

    it('should throw error for duplicate phone', async () => {
      const userData = {
        phone: '13800138000',
        nickname: '测试用户'
      };

      await userService.createUser(userData);

      await expect(userService.createUser(userData))
        .rejects
        .toThrow('Phone number already exists');
    });

    it('should validate phone format', async () => {
      const userData = {
        phone: 'invalid-phone',
        nickname: '测试用户'
      };

      await expect(userService.createUser(userData))
        .rejects
        .toThrow('Invalid phone number');
    });
  });

  describe('getUserById', () => {
    it('should return user by id', async () => {
      const userData = {
        phone: '13800138000',
        nickname: '测试用户'
      };

      const createdUser = await userService.createUser(userData);
      const foundUser = await userService.getUserById(createdUser.id);

      expect(foundUser).toBeDefined();
      expect(foundUser.id).toBe(createdUser.id);
      expect(foundUser.phone).toBe(userData.phone);
    });

    it('should return null for non-existent user', async () => {
      const user = await userService.getUserById('non-existent-id');

      expect(user).toBeNull();
    });
  });

  describe('updateUser', () => {
    it('should update user data', async () => {
      const userData = {
        phone: '13800138000',
        nickname: '测试用户'
      };

      const createdUser = await userService.createUser(userData);
      const updateData = {
        nickname: '更新后的昵称',
        persona: 'gentle'
      };

      const updatedUser = await userService.updateUser(createdUser.id, updateData);

      expect(updatedUser.nickname).toBe(updateData.nickname);
      expect(updatedUser.persona).toBe(updateData.persona);
    });

    it('should throw error for non-existent user', async () => {
      const updateData = {
        nickname: '更新后的昵称'
      };

      await expect(userService.updateUser('non-existent-id', updateData))
        .rejects
        .toThrow('User not found');
    });
  });

  describe('deleteUser', () => {
    it('should delete user', async () => {
      const userData = {
        phone: '13800138000',
        nickname: '测试用户'
      };

      const createdUser = await userService.createUser(userData);
      await userService.deleteUser(createdUser.id);

      const foundUser = await userService.getUserById(createdUser.id);
      expect(foundUser).toBeNull();
    });

    it('should throw error for non-existent user', async () => {
      await expect(userService.deleteUser('non-existent-id'))
        .rejects
        .toThrow('User not found');
    });
  });
});
```

### 2.4 控制器层测试

```javascript
// tests/controllers/userController.test.js
const request = require('supertest');
const app = require('../../src/app');
const { prisma } = require('../setup');

describe('UserController', () => {
  let authToken;
  let userId;

  beforeEach(async () => {
    // 创建测试用户
    const response = await request(app)
      .post('/api/auth/register')
      .send({
        phone: '13800138000',
        code: '888888'
      });

    authToken = response.body.accessToken;
    userId = response.body.user.id;
  });

  describe('GET /api/user/profile', () => {
    it('should return user profile', async () => {
      const response = await request(app)
        .get('/api/user/profile')
        .set('Authorization', `Bearer ${authToken}`)
        .expect(200);

      expect(response.body).toBeDefined();
      expect(response.body.id).toBe(userId);
      expect(response.body.phone).toBe('138****8000'); // 脱敏
    });

    it('should return 401 without auth token', async () => {
      await request(app)
        .get('/api/user/profile')
        .expect(401);
    });

    it('should return 401 with invalid token', async () => {
      await request(app)
        .get('/api/user/profile')
        .set('Authorization', 'Bearer invalid-token')
        .expect(401);
    });
  });

  describe('PUT /api/user/profile', () => {
    it('should update user profile', async () => {
      const updateData = {
        nickname: '新昵称',
        persona: 'gentle'
      };

      const response = await request(app)
        .put('/api/user/profile')
        .set('Authorization', `Bearer ${authToken}`)
        .send(updateData)
        .expect(200);

      expect(response.body.nickname).toBe(updateData.nickname);
      expect(response.body.persona).toBe(updateData.persona);
    });

    it('should validate input data', async () => {
      const invalidData = {
        nickname: '', // 空昵称
        persona: 'invalid-persona' // 无效人格
      };

      await request(app)
        .put('/api/user/profile')
        .set('Authorization', `Bearer ${authToken}`)
        .send(invalidData)
        .expect(400);
    });
  });
});
```

### 2.5 中间件测试

```javascript
// tests/middleware/auth.test.js
const { authenticate, authorize } = require('../../src/middleware/auth');
const jwtService = require('../../src/services/jwtService');

describe('Auth Middleware', () => {
  let req, res, next;

  beforeEach(() => {
    req = {
      headers: {}
    };
    res = {
      status: jest.fn().mockReturnThis(),
      json: jest.fn()
    };
    next = jest.fn();
  });

  describe('authenticate', () => {
    it('should call next for valid token', async () => {
      const user = { id: 'user-123', phone: '13800138000' };
      const token = jwtService.generateAccessToken(user);
      
      req.headers.authorization = `Bearer ${token}`;

      await authenticate(req, res, next);

      expect(next).toHaveBeenCalled();
      expect(req.user).toBeDefined();
      expect(req.user.userId).toBe(user.id);
    });

    it('should return 401 for missing token', async () => {
      await authenticate(req, res, next);

      expect(res.status).toHaveBeenCalledWith(401);
      expect(res.json).toHaveBeenCalledWith({
        error: 'Unauthorized',
        code: 'MISSING_TOKEN'
      });
      expect(next).not.toHaveBeenCalled();
    });

    it('should return 401 for invalid token', async () => {
      req.headers.authorization = 'Bearer invalid-token';

      await authenticate(req, res, next);

      expect(res.status).toHaveBeenCalledWith(401);
      expect(res.json).toHaveBeenCalledWith({
        error: 'Unauthorized',
        code: 'INVALID_TOKEN'
      });
      expect(next).not.toHaveBeenCalled();
    });
  });

  describe('authorize', () => {
    it('should call next for authorized role', () => {
      req.user = { role: 'admin' };
      const middleware = authorize('admin');

      middleware(req, res, next);

      expect(next).toHaveBeenCalled();
    });

    it('should return 403 for unauthorized role', () => {
      req.user = { role: 'user' };
      const middleware = authorize('admin');

      middleware(req, res, next);

      expect(res.status).toHaveBeenCalledWith(403);
      expect(res.json).toHaveBeenCalledWith({
        error: 'Forbidden',
        code: 'INSUFFICIENT_PERMISSIONS'
      });
      expect(next).not.toHaveBeenCalled();
    });
  });
});
```

---

## 三、集成测试

### 3.1 数据库集成测试

```javascript
// tests/integration/database.test.js
const { prisma } = require('../setup');

describe('Database Integration', () => {
  describe('User CRUD Operations', () => {
    it('should create user with all fields', async () => {
      const userData = {
        phone: '13800138000',
        nickname: '测试用户',
        persona: 'toxic',
        is_vip: false
      };

      const user = await prisma.user.create({
        data: userData
      });

      expect(user).toBeDefined();
      expect(user.id).toBeDefined();
      expect(user.phone).toBe(userData.phone);
      expect(user.nickname).toBe(userData.nickname);
      expect(user.persona).toBe(userData.persona);
      expect(user.is_vip).toBe(userData.is_vip);
      expect(user.created_at).toBeDefined();
    });

    it('should enforce unique phone constraint', async () => {
      const userData = {
        phone: '13800138000',
        nickname: '测试用户'
      };

      await prisma.user.create({ data: userData });

      await expect(prisma.user.create({ data: userData }))
        .rejects
        .toThrow();
    });

    it('should cascade delete user conversations', async () => {
      const user = await prisma.user.create({
        data: { phone: '13800138000', nickname: '测试用户' }
      });

      const conversation = await prisma.conversation.create({
        data: {
          user_id: user.id,
          title: '测试会话'
        }
      });

      await prisma.user.delete({ where: { id: user.id } });

      const foundConversation = await prisma.conversation.findUnique({
        where: { id: conversation.id }
      });

      expect(foundConversation).toBeNull();
    });
  });

  describe('Conversation Operations', () => {
    it('should create conversation with messages', async () => {
      const user = await prisma.user.create({
        data: { phone: '13800138000', nickname: '测试用户' }
      });

      const conversation = await prisma.conversation.create({
        data: {
          user_id: user.id,
          title: '测试会话'
        }
      });

      const message = await prisma.message.create({
        data: {
          conversation_id: conversation.id,
          role: 'user',
          content: '你好',
          emotion: 'happy',
          importance: 5
        }
      });

      expect(message).toBeDefined();
      expect(message.conversation_id).toBe(conversation.id);
    });

    it('should query messages with pagination', async () => {
      const user = await prisma.user.create({
        data: { phone: '13800138000', nickname: '测试用户' }
      });

      const conversation = await prisma.conversation.create({
        data: {
          user_id: user.id,
          title: '测试会话'
        }
      });

      // 创建多条消息
      for (let i = 0; i < 20; i++) {
        await prisma.message.create({
          data: {
            conversation_id: conversation.id,
            role: i % 2 === 0 ? 'user' : 'assistant',
            content: `消息 ${i}`,
            importance: Math.floor(Math.random() * 10) + 1
          }
        });
      }

      // 分页查询
      const messages = await prisma.message.findMany({
        where: { conversation_id: conversation.id },
        orderBy: { created_at: 'desc' },
        take: 10,
        skip: 0
      });

      expect(messages).toHaveLength(10);
      expect(messages[0].content).toBe('消息 19');
    });
  });
});
```

### 3.2 API集成测试

```javascript
// tests/integration/api.test.js
const request = require('supertest');
const app = require('../../src/app');
const { prisma } = require('../setup');

describe('API Integration', () => {
  let authToken;
  let userId;

  beforeEach(async () => {
    // 注册用户
    const response = await request(app)
      .post('/api/auth/register')
      .send({
        phone: '13800138000',
        code: '888888'
      });

    authToken = response.body.accessToken;
    userId = response.body.user.id;
  });

  describe('Authentication Flow', () => {
    it('should complete full auth flow', async () => {
      // 1. 发送验证码
      await request(app)
        .post('/api/auth/send-code')
        .send({ phone: '13900139000' })
        .expect(200);

      // 2. 验证码登录
      const loginResponse = await request(app)
        .post('/api/auth/login')
        .send({
          phone: '13900139000',
          code: '888888'
        })
        .expect(200);

      expect(loginResponse.body.accessToken).toBeDefined();
      expect(loginResponse.body.refreshToken).toBeDefined();

      // 3. 使用token访问受保护资源
      const profileResponse = await request(app)
        .get('/api/user/profile')
        .set('Authorization', `Bearer ${loginResponse.body.accessToken}`)
        .expect(200);

      expect(profileResponse.body.phone).toBeDefined();

      // 4. 刷新token
      const refreshResponse = await request(app)
        .post('/api/auth/refresh')
        .send({ refreshToken: loginResponse.body.refreshToken })
        .expect(200);

      expect(refreshResponse.body.accessToken).toBeDefined();

      // 5. 登出
      await request(app)
        .post('/api/auth/logout')
        .set('Authorization', `Bearer ${refreshResponse.body.accessToken}`)
        .expect(200);
    });
  });

  describe('Chat Flow', () => {
    it('should complete full chat flow', async () => {
      // 1. 创建会话
      const conversationResponse = await request(app)
        .post('/api/chat/conversations')
        .set('Authorization', `Bearer ${authToken}`)
        .send({ title: '测试会话' })
        .expect(201);

      const conversationId = conversationResponse.body.id;

      // 2. 发送消息
      const messageResponse = await request(app)
        .post(`/api/chat/conversations/${conversationId}/messages`)
        .set('Authorization', `Bearer ${authToken}`)
        .send({ content: '你好' })
        .expect(201);

      expect(messageResponse.body.content).toBeDefined();
      expect(messageResponse.body.role).toBe('user');

      // 3. 获取会话详情
      const conversationDetail = await request(app)
        .get(`/api/chat/conversations/${conversationId}`)
        .set('Authorization', `Bearer ${authToken}`)
        .expect(200);

      expect(conversationDetail.body.messages).toBeDefined();
      expect(conversationDetail.body.messages.length).toBeGreaterThan(0);

      // 4. 删除会话
      await request(app)
        .delete(`/api/chat/conversations/${conversationId}`)
        .set('Authorization', `Bearer ${authToken}`)
        .expect(200);
    });
  });

  describe('Tools Flow', () => {
    it('should manage todos', async () => {
      // 1. 创建待办
      const todoResponse = await request(app)
        .post('/api/tools/todos')
        .set('Authorization', `Bearer ${authToken}`)
        .send({
          content: '测试待办',
          due_date: '2026-12-31'
        })
        .expect(201);

      const todoId = todoResponse.body.id;

      // 2. 获取待办列表
      const todosResponse = await request(app)
        .get('/api/tools/todos')
        .set('Authorization', `Bearer ${authToken}`)
        .expect(200);

      expect(todosResponse.body.length).toBeGreaterThan(0);

      // 3. 更新待办
      await request(app)
        .put(`/api/tools/todos/${todoId}`)
        .set('Authorization', `Bearer ${authToken}`)
        .send({ is_done: true })
        .expect(200);

      // 4. 删除待办
      await request(app)
        .delete(`/api/tools/todos/${todoId}`)
        .set('Authorization', `Bearer ${authToken}`)
        .expect(200);
    });
  });
});
```

---

## 四、端到端测试

### 4.1 Playwright配置

```javascript
// playwright.config.js
const { defineConfig, devices } = require('@playwright/test');

module.exports = defineConfig({
  testDir: './e2e',
  fullyParallel: true,
  forbidOnly: !!process.env.CI,
  retries: process.env.CI ? 2 : 0,
  workers: process.env.CI ? 1 : undefined,
  reporter: 'html',
  use: {
    baseURL: 'http://localhost:5173',
    trace: 'on-first-retry',
    screenshot: 'only-on-failure'
  },
  projects: [
    {
      name: 'chromium',
      use: { ...devices['Desktop Chrome'] }
    },
    {
      name: 'firefox',
      use: { ...devices['Desktop Firefox'] }
    },
    {
      name: 'webkit',
      use: { ...devices['Desktop Safari'] }
    },
    {
      name: 'Mobile Chrome',
      use: { ...devices['Pixel 5'] }
    },
    {
      name: 'Mobile Safari',
      use: { ...devices['iPhone 12'] }
    }
  ],
  webServer: {
    command: 'npm run dev',
    url: 'http://localhost:5173',
    reuseExistingServer: !process.env.CI
  }
});
```

### 4.2 E2E测试用例

```javascript
// e2e/auth.spec.js
const { test, expect } = require('@playwright/test');

test.describe('Authentication', () => {
  test('should login successfully', async ({ page }) => {
    // 访问登录页
    await page.goto('/login');

    // 输入手机号
    await page.fill('[data-testid="phone-input"]', '13800138000');

    // 点击发送验证码
    await page.click('[data-testid="send-code-button"]');

    // 等待验证码输入框出现
    await page.waitForSelector('[data-testid="code-input"]');

    // 输入验证码
    await page.fill('[data-testid="code-input"]', '888888');

    // 点击登录
    await page.click('[data-testid="login-button"]');

    // 验证跳转到首页
    await expect(page).toHaveURL('/');

    // 验证用户信息显示
    await expect(page.locator('[data-testid="user-avatar"]')).toBeVisible();
  });

  test('should show error for invalid phone', async ({ page }) => {
    await page.goto('/login');

    await page.fill('[data-testid="phone-input"]', 'invalid-phone');
    await page.click('[data-testid="send-code-button"]');

    // 验证错误提示
    await expect(page.locator('[data-testid="error-message"]'))
      .toContainText('请输入正确的手机号');
  });
});

// e2e/chat.spec.js
const { test, expect } = require('@playwright/test');

test.describe('Chat', () => {
  test.beforeEach(async ({ page }) => {
    // 登录
    await page.goto('/login');
    await page.fill('[data-testid="phone-input"]', '13800138000');
    await page.click('[data-testid="send-code-button"]');
    await page.waitForSelector('[data-testid="code-input"]');
    await page.fill('[data-testid="code-input"]', '888888');
    await page.click('[data-testid="login-button"]');
    await page.waitForURL('/');
  });

  test('should send and receive messages', async ({ page }) => {
    // 点击新建会话
    await page.click('[data-testid="new-chat-button"]');

    // 输入消息
    await page.fill('[data-testid="message-input"]', '你好');
    await page.click('[data-testid="send-button"]');

    // 验证消息发送成功
    await expect(page.locator('[data-testid="message-list"]'))
      .toContainText('你好');

    // 等待AI回复
    await page.waitForSelector('[data-testid="ai-message"]', { timeout: 10000 });

    // 验证AI回复
    const aiMessages = await page.locator('[data-testid="ai-message"]').count();
    expect(aiMessages).toBeGreaterThan(0);
  });

  test('should create and manage conversations', async ({ page }) => {
    // 创建新会话
    await page.click('[data-testid="new-chat-button"]');
    await page.fill('[data-testid="message-input"]', '第一条消息');
    await page.click('[data-testid="send-button"]');

    // 验证会话列表
    await page.click('[data-testid="conversation-list"]');
    await expect(page.locator('[data-testid="conversation-item"]'))
      .toHaveCount(1);

    // 创建第二个会话
    await page.click('[data-testid="new-chat-button"]');
    await page.fill('[data-testid="message-input"]', '第二条消息');
    await page.click('[data-testid="send-button"]');

    // 验证会话列表更新
    await page.click('[data-testid="conversation-list"]');
    await expect(page.locator('[data-testid="conversation-item"]'))
      .toHaveCount(2);
  });
});

// e2e/tools.spec.js
const { test, expect } = require('@playwright/test');

test.describe('Tools', () => {
  test.beforeEach(async ({ page }) => {
    // 登录
    await page.goto('/login');
    await page.fill('[data-testid="phone-input"]', '13800138000');
    await page.click('[data-testid="send-code-button"]');
    await page.waitForSelector('[data-testid="code-input"]');
    await page.fill('[data-testid="code-input"]', '888888');
    await page.click('[data-testid="login-button"]');
    await page.waitForURL('/');
  });

  test('should manage todos', async ({ page }) => {
    // 导航到待办页面
    await page.click('[data-testid="tools-tab"]');
    await page.click('[data-testid="todos-link"]');

    // 创建待办
    await page.fill('[data-testid="todo-input"]', '测试待办');
    await page.click('[data-testid="add-todo-button"]');

    // 验证待办创建成功
    await expect(page.locator('[data-testid="todo-item"]'))
      .toContainText('测试待办');

    // 完成待办
    await page.click('[data-testid="todo-checkbox"]');
    await expect(page.locator('[data-testid="todo-item"]'))
      .toHaveClass(/completed/);
  });

  test('should manage countdowns', async ({ page }) => {
    // 导航到倒数日页面
    await page.click('[data-testid="tools-tab"]');
    await page.click('[data-testid="countdowns-link"]');

    // 创建倒数日
    await page.fill('[data-testid="countdown-title"]', '生日');
    await page.fill('[data-testid="countdown-date"]', '2026-12-31');
    await page.click('[data-testid="add-countdown-button"]');

    // 验证倒数日创建成功
    await expect(page.locator('[data-testid="countdown-item"]'))
      .toContainText('生日');
  });
});
```

---

## 五、性能测试

### 5.1 k6配置

```javascript
// performance/load-test.js
import http from 'k6/http';
import { check, sleep } from 'k6';
import { Rate, Trend } from 'k6/metrics';

// 自定义指标
const errorRate = new Rate('errors');
const latency = new Trend('latency');

// 测试配置
export const options = {
  stages: [
    { duration: '2m', target: 100 },  // 逐步增加到100用户
    { duration: '5m', target: 100 },  // 保持100用户
    { duration: '2m', target: 200 },  // 增加到200用户
    { duration: '5m', target: 200 },  // 保持200用户
    { duration: '2m', target: 0 },    // 逐步减少到0
  ],
  thresholds: {
    http_req_duration: ['p(95)<200'],  // 95%请求小于200ms
    errors: ['rate<0.1'],              // 错误率小于10%
    latency: ['p(95)<200']             // 95%延迟小于200ms
  }
};

// 测试数据
const BASE_URL = __ENV.BASE_URL || 'http://localhost:3000';
const AUTH_TOKEN = __ENV.AUTH_TOKEN || '';

// 测试场景
export default function () {
  // 获取用户信息
  const profileResponse = http.get(`${BASE_URL}/api/user/profile`, {
    headers: { Authorization: `Bearer ${AUTH_TOKEN}` }
  });

  check(profileResponse, {
    'profile status is 200': (r) => r.status === 200,
    'profile response time < 200ms': (r) => r.timings.duration < 200
  }) || errorRate.add(1);

  latency.add(profileResponse.timings.duration);

  sleep(1);

  // 获取会话列表
  const conversationsResponse = http.get(`${BASE_URL}/api/chat/conversations`, {
    headers: { Authorization: `Bearer ${AUTH_TOKEN}` }
  });

  check(conversationsResponse, {
    'conversations status is 200': (r) => r.status === 200,
    'conversations response time < 200ms': (r) => r.timings.duration < 200
  }) || errorRate.add(1);

  sleep(1);

  // 发送消息
  const messageResponse = http.post(
    `${BASE_URL}/api/chat/conversations/test/messages`,
    JSON.stringify({ content: '测试消息' }),
    {
      headers: {
        Authorization: `Bearer ${AUTH_TOKEN}`,
        'Content-Type': 'application/json'
      }
    }
  );

  check(messageResponse, {
    'message status is 201': (r) => r.status === 201,
    'message response time < 1000ms': (r) => r.timings.duration < 1000
  }) || errorRate.add(1);

  sleep(2);
}
```

### 5.2 压力测试

```javascript
// performance/stress-test.js
import http from 'k6/http';
import { check, sleep } from 'k6';
import { Rate, Trend } from 'k6/metrics';

const errorRate = new Rate('errors');
const latency = new Trend('latency');

export const options = {
  stages: [
    { duration: '1m', target: 500 },   // 快速增加到500用户
    { duration: '3m', target: 500 },   // 保持500用户
    { duration: '1m', target: 1000 },  // 增加到1000用户
    { duration: '3m', target: 1000 },  // 保持1000用户
    { duration: '1m', target: 0 }      // 逐步减少
  ],
  thresholds: {
    http_req_duration: ['p(95)<500'],  // 95%请求小于500ms
    errors: ['rate<0.2'],              // 错误率小于20%
    latency: ['p(95)<500']
  }
};

export default function () {
  const BASE_URL = __ENV.BASE_URL || 'http://localhost:3000';
  const AUTH_TOKEN = __ENV.AUTH_TOKEN || '';

  // 模拟真实用户行为
  const responses = http.batch([
    ['GET', `${BASE_URL}/api/user/profile`, null, {
      headers: { Authorization: `Bearer ${AUTH_TOKEN}` }
    }],
    ['GET', `${BASE_URL}/api/chat/conversations`, null, {
      headers: { Authorization: `Bearer ${AUTH_TOKEN}` }
    }],
    ['GET', `${BASE_URL}/api/tools/todos`, null, {
      headers: { Authorization: `Bearer ${AUTH_TOKEN}` }
    }]
  ]);

  responses.forEach((response, index) => {
    check(response, {
      [`batch ${index} status is 200`]: (r) => r.status === 200,
      [`batch ${index} response time < 500ms`]: (r) => r.timings.duration < 500
    }) || errorRate.add(1);

    latency.add(response.timings.duration);
  });

  sleep(1);
}
```

---

## 六、安全测试

### 6.1 OWASP ZAP配置

```yaml
# zap-config.yaml
env:
  contexts:
    - name: "Cyber Sister API"
      urls:
        - "http://localhost:3000/api"
      includePaths:
        - "http://localhost:3000/api/.*"
      excludePaths:
        - "http://localhost:3000/api/health"
      authentication:
        method: "bearer"
        parameters:
          token: "${AUTH_TOKEN}"
        verification:
          method: "response"
          pattern: "200"

  parameters:
    failOnError: true
    progressToStdout: true

  policies:
    - name: "SQL Injection"
      strength: "high"
      threshold: "low"
    - name: "Cross Site Scripting"
      strength: "high"
      threshold: "low"
    - name: "Path Traversal"
      strength: "high"
      threshold: "low"
    - name: "Remote File Inclusion"
      strength: "high"
      threshold: "low"
```

### 6.2 安全测试脚本

```javascript
// tests/security/security.test.js
const request = require('supertest');
const app = require('../../src/app');

describe('Security Tests', () => {
  describe('SQL Injection', () => {
    it('should prevent SQL injection in login', async () => {
      const maliciousPayload = {
        phone: "13800138000' OR '1'='1",
        code: '888888'
      };

      const response = await request(app)
        .post('/api/auth/login')
        .send(maliciousPayload)
        .expect(400);

      expect(response.body.error).toContain('Invalid phone number');
    });

    it('should prevent SQL injection in search', async () => {
      const maliciousQuery = "'; DROP TABLE users; --";

      const response = await request(app)
        .get(`/api/memories?search=${encodeURIComponent(maliciousQuery)}`)
        .set('Authorization', 'Bearer valid-token')
        .expect(400);
    });
  });

  describe('XSS Prevention', () => {
    it('should sanitize user input', async () => {
      const maliciousInput = {
        nickname: '<script>alert("xss")</script>',
        content: '<img src=x onerror=alert("xss")>'
      };

      const response = await request(app)
        .put('/api/user/profile')
        .set('Authorization', 'Bearer valid-token')
        .send(maliciousInput)
        .expect(200);

      expect(response.body.nickname).not.toContain('<script>');
      expect(response.body.nickname).not.toContain('onerror');
    });
  });

  describe('Authentication Bypass', () => {
    it('should not accept mock tokens in production', async () => {
      const response = await request(app)
        .get('/api/user/profile')
        .set('Authorization', 'Bearer mock-token-123')
        .expect(401);

      expect(response.body.code).toBe('INVALID_TOKEN');
    });

    it('should not accept expired tokens', async () => {
      const expiredToken = 'eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJ1c2VySWQiOiJ1c2VyLTEyMyIsImlhdCI6MTY0MDk5NTIwMCwiZXhwIjoxNjQwOTk1MjAwfQ.invalid';

      const response = await request(app)
        .get('/api/user/profile')
        .set('Authorization', `Bearer ${expiredToken}`)
        .expect(401);
    });
  });

  describe('Rate Limiting', () => {
    it('should enforce rate limits', async () => {
      const requests = [];

      for (let i = 0; i < 101; i++) {
        requests.push(
          request(app)
            .get('/api/user/profile')
            .set('Authorization', 'Bearer valid-token')
        );
      }

      const responses = await Promise.all(requests);
      const rateLimited = responses.filter(r => r.status === 429);

      expect(rateLimited.length).toBeGreaterThan(0);
    });
  });

  describe('Input Validation', () => {
    it('should validate phone format', async () => {
      const invalidPhones = [
        '1234567890',  // 无效前缀
        '1380013800',  // 少一位
        '138001380000', // 多一位
        'abcdefghijk'  // 非数字
      ];

      for (const phone of invalidPhones) {
        const response = await request(app)
          .post('/api/auth/send-code')
          .send({ phone })
          .expect(400);

        expect(response.body.error).toBeDefined();
      }
    });

    it('should validate message length', async () => {
      const longMessage = 'a'.repeat(10001);

      const response = await request(app)
        .post('/api/chat/conversations/test/messages')
        .set('Authorization', 'Bearer valid-token')
        .send({ content: longMessage })
        .expect(400);

      expect(response.body.error).toContain('too long');
    });
  });
});
```

---

## 七、CI/CD集成

### 7.1 GitHub Actions测试流水线

```yaml
# .github/workflows/test.yml
name: Test Pipeline

on:
  push:
    branches: [main, develop]
  pull_request:
    branches: [main]

jobs:
  unit-tests:
    runs-on: ubuntu-latest
    
    steps:
    - uses: actions/checkout@v3
    
    - name: Setup Node.js
      uses: actions/setup-node@v3
      with:
        node-version: '20'
        cache: 'npm'
    
    - name: Install dependencies
      run: npm ci
    
    - name: Run linting
      run: npm run lint
    
    - name: Run unit tests
      run: npm test -- --coverage
    
    - name: Upload coverage
      uses: codecov/codecov-action@v3
      with:
        file: ./coverage/lcov.info

  integration-tests:
    runs-on: ubuntu-latest
    needs: unit-tests
    
    services:
      postgres:
        image: postgres:16
        env:
          POSTGRES_USER: test
          POSTGRES_PASSWORD: test
          POSTGRES_DB: cyber_sister_test
        ports:
          - 5432:5432
        options: >-
          --health-cmd pg_isready
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5
      
      redis:
        image: redis:7
        ports:
          - 6379:6379
        options: >-
          --health-cmd "redis-cli ping"
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5
    
    steps:
    - uses: actions/checkout@v3
    
    - name: Setup Node.js
      uses: actions/setup-node@v3
      with:
        node-version: '20'
        cache: 'npm'
    
    - name: Install dependencies
      run: npm ci
    
    - name: Run integration tests
      run: npm run test:integration
      env:
        DATABASE_URL: postgresql://test:test@localhost:5432/cyber_sister_test
        REDIS_URL: redis://localhost:6379

  e2e-tests:
    runs-on: ubuntu-latest
    needs: integration-tests
    
    steps:
    - uses: actions/checkout@v3
    
    - name: Setup Node.js
      uses: actions/setup-node@v3
      with:
        node-version: '20'
        cache: 'npm'
    
    - name: Install dependencies
      run: npm ci
    
    - name: Install Playwright
      run: npx playwright install --with-deps
    
    - name: Run E2E tests
      run: npx playwright test
    
    - name: Upload test results
      uses: actions/upload-artifact@v3
      if: always()
      with:
        name: playwright-report
        path: playwright-report/
        retention-days: 30

  performance-tests:
    runs-on: ubuntu-latest
    needs: e2e-tests
    
    steps:
    - uses: actions/checkout@v3
    
    - name: Setup k6
      uses: grafana/k6-action@v3
      with:
        filename: performance/load-test.js
      env:
        BASE_URL: ${{ secrets.STAGING_URL }}
        AUTH_TOKEN: ${{ secrets.TEST_AUTH_TOKEN }}
    
    - name: Upload performance results
      uses: actions/upload-artifact@v3
      with:
        name: k6-results
        path: k6-results/
```

---

## 八、总结

本测试与质量保证方案通过单元测试、集成测试、端到端测试、性能测试、安全测试，实现了：

1. **全面覆盖**：单元测试覆盖率≥80%，集成测试覆盖率≥70%
2. **自动化测试**：CI/CD集成，自动运行测试
3. **性能保证**：P95<200ms，压力测试支持1000+并发
4. **安全防护**：SQL注入、XSS、认证绕过等安全测试
5. **质量监控**：测试报告、覆盖率报告、性能报告

该架构可支持DAU 5万的目标，并具备向更高规模扩展的能力。