# 能力扩展边界（原「本地优先与能力扩展边界」，2026-09-07 云端切割后修订）

本文记录当前代码必须保持的两个扩展面。它不是动态插件系统，也不允许功能以“规划中”状态冒充可用能力。

## 1. 模型扩展面

2026-09-07 云端切割后，聊天与解释只有**云端**一条路径（供应商槽 `GATEWAY_QWEN_*`，供应商中立），llama.cpp 本地 provider 面已删除。所有模型调用经 `@cyber-sister/llm-gateway` 进入，provider 必须声明：

- `scope: external`：数据离开部署信任边界（切割后恒为 external；保留该字段以兼容网关契约）。
- `scenes`：provider 可以服务的业务场景，如 `chat`、`explain`。
- OpenAI-compatible `baseUrl`、`model` 与超时等运行参数，凭据只来自部署环境的只读 secret 文件。

云端调用必须先经用户同意：调用方必须显式传入 `allowExternal`（由同意门装配）与 `authorizeExternal`（每次调用前重读数据库同意状态）。未同意时云端调用次数为零。供应商地址、模型与凭据不从页面接收。

聊天不得各自实现供应商选择；所有模型调用统一经主 API 执行同意门、路由、来源标记与日志约束。

## 2. 产品能力扩展面

主 Web 的能力注册表是导航和展示的唯一来源。每项能力至少声明：

- 稳定 `id` 与显示名称。
- `status: available | planned | unavailable`。
- 已上线能力的同源路由。
- 一句可验证的隐私说明。
- 视觉语义色；组件不自带裸色值。

只有 `available` 能导航。`planned` 只能显示“规划中”，不得出现上传、分析、支付或生成等伪交互。

新增独立体验（如新照片/媒体类能力）时：

1. 在 `packages/<capability>` 内拥有自己的前端与领域代码。
2. 复用共享设计令牌、主应用 access token 和主 API 模型路由。
3. 在 Nginx 下使用独立同源前缀，媒体处理默认只在浏览器进行。
4. 服务端只接受规范化、白名单化的结构化特征；若确需上传媒体，必须单独更新隐私合同和测试门禁。
5. 完成真实 E2E 后才把注册状态切换为 `available`。

   登记（2026-09-07）：虚拟化妆间/虚拟试衣间（本机 ComfyUI 生图）已随云端切割移除下线——云端部署下「本机」语义不再成立，服务器侧生图与多租户共享主机不可兼得。上方两条历史登记（2026-08-30 例外、2026-09-05 隐私合同登记）随之作废。当前唯一照片类能力是美颜相机（MediaPipe WASM 全程浏览器内处理）。服务器侧执行能力（bash_run、内置浏览器、工作模式生图、SKILL.md 技能、JS 插件、MCP）同步删除，禁止以扩展名义复活。

   登记（2026-09-08）：能力注册表更新为两项——**化妆间**（原美颜相机升级改名 `/tools/makeup-room`：MediaPipe WASM 浏览器内实时处理 + 自定义妆容预设 `/api/makeup-presets` 服务端落库，预设只含四参数与名字、不含照片）与 **3D 衣柜**（`/tools/wardrobe`：单品照上传服务器 → 外部图生 3D API → GLB 落库 + `<model-viewer>` 预览；外部服务接入位在 `services/imageTo3dService.js`，未配置 `IMAGE_TO_3D_API_URL` 时诚实 503 不落任何数据）。按 §2 第 4 条，3D 衣柜的媒体上传已同步更新隐私合同（隐私政策 §3.5）与测试门禁。工作模式新增「功能桌面」：全部功能入口的唯一网格（`components/work/WorkDesktop.jsx`），空态直出 + ChatHeader「功能」按钮唤出共用同一组件。

## 3. 共享 UI 基础

`packages/design-tokens/tokens.css` 是所有体验的颜色、焦点、圆角、阴影和动效基线。四个低饱和品牌色只用作表面；正文、操作、状态和危机反馈使用独立高对比语义色。

未来模块可以有自己的布局和任务颜色，但不得重新定义 AI 来源、隐私、本地、外部、警告和危险的语义。
