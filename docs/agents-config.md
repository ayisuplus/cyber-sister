# Agent Configuration

This file contains configuration for engineering skills and agent tools.

## Agent skills

### Issue tracker

Local markdown files in `.scratch/<feature>/` directory. See `docs/agents/issue-tracker.md`.

### Domain docs

Single-context layout with `CONTEXT.md` and `docs/adr/` at repo root. See `docs/agents/domain.md`.