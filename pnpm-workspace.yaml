packages:
  - 'apps/*'
  - 'packages/*'
# pnpm 11 供应链策略默认拦截 postinstall 构建脚本（strictDepBuilds）。
# v11 已移除 onlyBuiltDependencies，改用 allowBuilds 白名单：
allowBuilds:
  '@prisma/client': true
  '@prisma/engines': true
  prisma: true
  esbuild: true
