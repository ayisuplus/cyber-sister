# 交接计划（2026-09-22）

> 状态：✅ 交接计划里的三件事都办完了——第一件（本子）、第二件（自定义模型配置）、第三件（专为女生的那一层）均已实施并全量验证通过。这份文件写给下一位接手的人（下一轮的开工项见文末 §三 之后）。
> 先读这三处：
> - 根目录 `AGENTS.md` 与 `.coordination/project.md`：协作契约；
> - `docs/README.md`：文档索引；
> - `CHANGELOG.md` 最上面七条：最近做了什么（这一天的三件事就在最前面三条）。

## 〇、现在的状况

### 最近几轮已完成、全部测试通过，但都**没有提交，也没有部署**

这个目录不是 git 仓库。

| 日期 | 内容 | 在 CHANGELOG 里的条目 |
|---|---|---|
| 09-21 | 路线图第二步「她懂分寸」 | 「她懂分寸」 |
| 09-21 | 装扮从生成改成收藏 | 「装扮从生成改成收藏」 |
| 09-22 | 对话写在信纸上：手写字、横格、写满翻页 | 「对话写在信纸上」 |
| 09-22 | 对话装订成本子：封皮、封面、绕左边封线从右往左翻、宽屏也只一页 | 「对话装订成本子」 |
| 09-22 | 模型不再绑死一家厂商：实例管理员在设置页配多家 OpenAI 兼容供应商 | 「模型不再绑死一家厂商」 |
| 09-22 | 专为女生的那一层（第三步）：提示词共用前言、开场话题从她的线索里来、冻结的离线「懂你」检验集 | 「专为女生的那一层」 |

最后一次全量验证（2026-09-22，三件都落地之后跑的）：

- API 1166 项（另有 70 项真实 PostgreSQL 集成用例无库跳过；本机对着 Docker 里的 PostgreSQL 跑过一遍，70 项全过）、Web 637 项、网关 25 项单测通过；
- Playwright 76 项通过（桌面与手机两端）；
- typecheck、build 通过；
- lint：Web 18 条告警（上限 22），API 143 条（上限 146），均为 0 error。
- 已知环境噪音：并行直跑 API 单测时偶发 `[vitest-pool]: Worker forks emitted error`（无失败用例，重跑即过；与这套机器上同时跑 Docker/Vite/Playwright 有关）。

### 待上线的迁移，部署前按约定**先验证备份**

- `apps/api/src/prisma/migrations/20260921100000_knows_you`
- `20260921180000_cycle_tone`
- `20260921200000_collection`
- `20260922120000_model_providers`（本轮新增，纯新建表 `model_providers`）

### 待法务确认的事（不要代写法务正文）

- 隐私政策 §3.5 与服务条款第 41 行仍在描述已删除的美颜和 3D 生成；隐私政策顶上已有技术核查注记。
- 「聊天时让她顾及你的周期」的说明文字。
- 云端同意说明新加了日记心情、周期、收藏几项；同意版本号 `cloud-primary-v3` 要不要升。

### 本机此刻在跑的服务

- **Docker Desktop**：开发库容器 `cyber-sister-dev-postgres-1` 在跑，监听 127.0.0.1:5432；数据卷已执行到最新迁移（含 `20260922120000_model_providers`），里面还是那 2 个内测账号。
- **API：127.0.0.1:3000 正在跑**（为接真实厂商起的进程，`hub ps` 里名字是 `api`）。它连的是宿主机上的开发库，并且已经配好 DeepSeek 这一家：密钥在仓库外的 `../.local-secrets/deepseek_api_key`（未提交），库里的密文用 `../.local-secrets/model_config_key` 加解密。
- **Vite 没在跑**。想在浏览器里看界面就 `pnpm dev:web`；注意内测形态的 `CORS_ORIGIN` 必须是 https，浏览器直连 5173 会被 CORS 挡——这也是界面验证走 Playwright 打桩的原因（真实链路是用真 API + 真厂商另跑了一次，见 §二）。
- 停：结束占用 3000 端口的 node 进程；关库在仓库根执行 `docker compose -f compose.dev.yaml stop`。
- ⚠️ **那把 DeepSeek 密钥在聊天里明文出现过一次**，建议方便时在 DeepSeek 控制台轮换；轮换后到设置页把密钥重填一遍即可，主密钥与其余配置都不用动。

### 本地启动的要点

照 README「本地运行」，下面几条是踩过的坑：

- **界面用 http 打开**：`http://localhost:5173/chat`。本机的 API 与 Vite 都是纯 http，用 `https://` 打开会得到 `ERR_SSL_PROTOCOL_ERROR`（内测形态的 `CORS_ORIGIN` 必须是 https 只是**配置值**的校验，不影响浏览器）。Vite 自带 `/api → http://localhost:3000` 代理，浏览器只跟 Vite 说话，不碰 CORS。如果 Chrome 自动把地址升成 https，手动把 `http://` 打全，或对 localhost 关掉「始终使用安全连接」。
- **跑 API 的人要带上模型配置的环境变量**：库里的供应商密钥要靠主密钥解，缺了它这一轮就没有可用云端（如实报 `LLM_UNAVAILABLE`），不会退回环境变量槽。本地起 API 的最小环境见下（路径按本机实际改）：

  ```bash
  cd apps/api && \
  APP_ENV=internal NODE_ENV=development \
  DATABASE_HOST=127.0.0.1 DATABASE_PORT=5432 POSTGRES_USER=cyber_sister POSTGRES_DB=cyber_sister \
  DATABASE_PASSWORD_FILE=../.local-secrets/postgres_password \
  JWT_SECRET_FILE=../.local-secrets/jwt_secret JWT_REFRESH_SECRET_FILE=../.local-secrets/jwt_refresh_secret \
  INTERNAL_TEST_CODE_FILE=../.local-secrets/internal_test_code \
  INTERNAL_TEST_PHONES_FILE=../.local-secrets/internal_test_phones \
  INSTANCE_ADMIN_PHONES_FILE=../.local-secrets/instance_admin_phones \
  MODEL_CONFIG_KEY_FILE=../.local-secrets/model_config_key \
  CORS_ORIGIN=https://internal.example.test APP_DOMAIN=internal.example.test \
  BIND_ADDRESS=127.0.0.1 IMAGE_TAG=dev-local \
  node src/bootstrap.js
  ```

- **`NODE_ENV=test` 不会起监听**：`app.js` 在测试环境下跳过 `app.listen`（单测故意这样），所以想让 API 真对外服务，必须用 `development` 或 `production`。反过来，API 进程常驻时 Windows 会锁住 Prisma 的查询引擎，`pnpm test` 的 `pretest`（`prisma generate`）会报 `EPERM: unlink query_engine-windows.dll.node`——跑测试改用 `npx vitest run`，或先停掉 API。
- **数据库地址**：`DATABASE_HOST` 用 `127.0.0.1`。用 `localhost` 时 Prisma 连不上容器（这台机器上会解析到 IPv6）。
- **Prisma 客户端**：改过 `schema.prisma` 后，要在 `apps/api` 下执行 `node_modules/.bin/prisma generate --schema src/prisma/schema.prisma`，否则 API 一查新表就报错。**不要**在 `apps/` 目录直接用 `npx prisma`：它会从网上下载 prisma。
- **聊天模型**：接口地址 `GATEWAY_QWEN_BASE_URL` 和模型名 `GATEWAY_QWEN_MODEL` 在 `../.local-secrets/aliyun-chat-20260915/runtime.env` 里，key 用 `GATEWAY_QWEN_API_KEY_FILE=../.local-secrets/qwen_api_key`。**不要把任何密钥打印出来。**
- **终端代理**：终端里设了 `HTTP_PROXY`，curl 测本地服务时要加 `--noproxy '*'`，否则会得到 502。
- **登录**：用内测白名单手机号加固定码，见 `../.local-secrets/internal_test_phones` 和 `internal_test_code`。

### 协作规矩（摘要，以 AGENTS.md 为准）

**先问再做**：

- 下载依赖、调用花钱的云端接口；
- 提交、推送、部署；
- 改生产库、做破坏性操作。

**文档与法务**：

- 迁移前先验证备份；
- 新增或删除文档要更新 `docs/README.md`；
- 法务文档签字前一律是「📝 草案待审」，也不代写。

**文件格式**：

- 行尾：很多文件是 CRLF，有些是 LF，改的时候保持原样；
- 用 bash heredoc 写含反斜杠的内容会被吞掉，改用编辑工具或脚本文件。

**测试**：

- e2e 要先 `node scripts/build-local.mjs` 构建，并且确认构建没报错再跑 Playwright；
- `local-features`、`night-mode` 两个 e2e 用的是严格模拟：新加的接口要补进它们的模拟表，否则会失败。

**产品原则**：安静、克制、高内聚。长期约定见 `docs/01-产品/开发计划与路线图.md` 的 C1–C11。

---

## 一、对话做成一个「本子」

> ✅ 已实施（2026-09-22）。落地见 CHANGELOG「对话装订成本子」、UI 规范 §9「本子」、路线图 C11 的追加裁定。宽屏摊开两页的问题按建议裁成**只摊开一页**（封线在左边，就是竖着的一本，本子居中、不超过 600px）。

### 用户原话（2026-09-22）

> 网页端的这个对话版式要优化，感觉边框都没有对齐。整个对话框我希望它是本子的感觉：左边应该有一个类似封线或者封皮的设定；翻页是从右往左翻；开头也有一个笔记本那种封面，就用我们的那个推荐页，就是默认页。

### 现在的实现

在 09-22 做的「信纸」之上：

| 文件 | 内容 |
|---|---|
| `apps/web/src/components/letter/LetterPad.jsx` | 纸、纸带、页码与翻页按钮 |
| `apps/web/src/components/letter/usePagedLetter.js` | 分页、翻页、滑动与键盘；翻页动画靠 `turnPage()` |
| `apps/web/src/components/letter/pagedLayout.js` | 页宽、页边线、行高等常量；宽于 840px 摊开两页 |
| `apps/web/src/components/letter/LetterEntry.jsx` | 纸上的每一段 |
| `apps/web/src/pages/ChatPage.jsx` | `EmptyLetter` 是空白第一页，也就是用户说的「推荐页 / 默认页」：问候、拱窗里的她、「嗨，我是你的Amie」、开场话题 `Openers` |
| `apps/web/src/styles/globals.css` | 「信纸」一节，从 `/* ===== 信纸（2026-09-22）` 开始 |
| `packages/design-tokens/tokens.css` | `--cs-paper*`、`--cs-ink-*`、`--cs-font-hand` |
| `docs/UI设计系统规范_V3.0.md` §9 | 信纸的设计说明 |

两处实现细节：

- **分页**：用 CSS 分栏，一栏就是一页，显示哪一页靠 `translateX` 平移纸带。
- **翻页动画**：把当前页 `cloneNode` 成一张「拓印」盖在上面让它滑走，底下已经是新的一页。
  - `.letter-ghost--forward` 往左滑出，`.letter-ghost--back` 往右滑出；
  - 用的是平移加一点倾斜，不是绕书脊翻。

### 要改成的样子

1. **边框对齐**：纸、页码那一行、最底下的书写行，要共用同一条页边线和同样的左右留白。

   现在没对齐的原因：
   - `InputBar` 自己带 `max-w-[880px] px-3 min-[641px]:px-5` 和内层圆角；
   - 页码行有 `border-top`；
   - 纸的顶上是 6px 圆角，底下的书写行又是另一套留白。

   做法：
   - 在 `.letter-writing` 下覆盖 InputBar 外层的 `max-width` 和 padding；
   - 让输入框左边对齐正文列（左边距 `--margin-x + --text-inset`）；
   - 页边线从纸顶一直画到书写行（背景画在 `.letter-sheet` 上，而不是只画在 `.letter-viewport` 上）；
   - 圆角统一只在本子外轮廓上。

   改完用截图核对，截图脚本见下面「怎么验」。

2. **本子的样子**：
   - **左边封皮**：`.letter-sheet` 左侧加一条 14–18px 的封皮，用比纸深的布纹或纸板色，加两道细缝线或一排装订孔，选一种，要安静，不要金属环那种花哨的；
   - 右下角有两三层很浅的纸边，暗示下面还有页；
   - 纸的阴影只在本子外轮廓；
   - 新增令牌 `--cs-cover`、`--cs-cover-stitch`，日间和夜间各一套，对比度要过 axe。

3. **从右往左翻**：把 `turnPage()` 改成绕左边封线翻页。
   - **往后翻**（下一页）：拓印的那一页 `transform-origin: left center`，从 `rotateY(0)` 到 `rotateY(-180deg)`：右边缘翘起，翻向左边。
     - 父层加 `perspective: 1600px`，拓印加 `backface-visibility: hidden`；
     - 背面用纸的反面色，可以用 `::after` 加一层淡阴影；
     - 约 700ms，减速，不回弹。
   - **往前翻**（上一页）：新的那一页要从左边翻回来盖上。
     - 做两层：先把旧页拓印成静止的底层；再把新页拓印成翻动层，从 `rotateY(-180deg)` 翻到 `0`；结束后两层都移除。
     - `turnPage()` 目前只拓印一次，这里要在 `setIndex` 之后的下一帧再拓印新页，可以用 `requestAnimationFrame` 或 `useLayoutEffect`。
   - `prefers-reduced-motion` 下仍然淡入淡出。
   - 保留现有约定：
     - 字体加载、版面重排引起的换页不播动画（`lastVersion` 机制）；
     - 拓印不可交互、对读屏隐藏；
     - 手机左滑是往后、右滑是往前；
     - ← → 和 PageUp / PageDown 翻页。
   - 性能：`rotateY` 只动 transform；低端机上如果掉帧，可以只拓印可见的那一栏（先复制出纸带，把 `transform` 设到当前页）。

4. **封面**：开头是一张笔记本封面，内容就是现在的 `EmptyLetter`：问候、拱窗里的她、「嗨，我是你的Amie」、开场话题。
   - 封面是第 0 页，放在所有信纸前面，样式用封皮色和纸板质感，不画横格；
   - 空对话时打开就是合上的本子（只看得到封面），点开场话题或在书写行写字，就翻开到第一页；
   - 有对话时，打开仍停在最新一页；一直往前翻，翻过最早的信（`hasOlder` 为 false 时）就回到封面；
   - 实现：`LetterPad` 增加一个 `cover` 插槽，渲染在纸带最前面，独占一栏：
     - 高度等于页高；
     - 用 `break-after: column` 让它独占第一栏；
     - 页码从封面之后算起，封面上页码写「封面」。
   - `Openers` 的行为不变：敏感话题只填草稿，不自动发送。

5. **宽屏两页摊开要不要保留**：需要问产品负责人。左边有封线，意味着是竖着的一本，所以**建议宽屏也只显示一页**：本子居中，宽度 560–640px。这样对齐最简单，也符合「左边封皮」。如果用户想保留摊开两页，封线就在中间书脊，左边是封面的边缘。

6. **不变的边界**：
   - 页头的「这是 AI，不是真人」、危机弹窗、首次使用的 AI 声明、两小时提醒不改；
   - 印刷体开关照旧（设置 → 外观 → 信纸上的字）。

### 要改的文件

- `LetterPad.jsx`、`usePagedLetter.js`、`pagedLayout.js`、`ChatPage.jsx`
- `globals.css` 信纸一节、`tokens.css`
- `InputBar.jsx`：只在需要时加一个 className 钩子，行为和 aria-label 不变

### 怎么验

- **单测**：`LetterPad.test.jsx` 里拓印的类名、方向要跟着改，并新增：
  - 封面是第 0 页，页码写「封面」；
  - 空对话停在封面；
  - 往前翻过最早一页回到封面；
  - 往后翻、往前翻的拓印都绕左边；
  - 减少动态效果时淡出。
- **e2e**：`apps/web/e2e/internal-smoke.spec.js` 里的「the letter …」和「one conversation …」两条跟着改：
  - 辅助函数 `letterPages`、`flipToFirstPage` 要能识别「封面」；
  - 其余照旧：axe、320px、夜间、印刷体。
- **截图**：在 `e2e/` 下临时写一个 spec，拍手机和桌面、日间和夜间、封面、翻到一半（`test.use({ contextOptions: { reducedMotion: 'no-preference' } })`，点击后等 250ms），拍完删掉。上一位就是这样发现字压线、提示语折行、翻页像一闪等问题的。
- **全量**：typecheck、lint（Web ≤ 22）、build、全部单测、Playwright 双端。
- **文档**：UI 规范 §9 改成「本子」、CHANGELOG、路线图 C11 追加这次的裁定、用户手册 §2.1。

---

## 二、自定义模型配置（不绑死在一家厂商）

> ✅ 已实施（2026-09-22）。产品负责人六项都按「推荐方案」裁定（实例管理员配 / 只做 OpenAI 兼容 / 多家按优先级依次切换 / 数据库加密存储 + `MODEL_CONFIG_KEY_FILE` 主密钥 / 向量模型本轮不动）。落地见 CHANGELOG「模型不再绑死一家厂商」、API 文档 §十·五、路线图 C12、部署手册「模型主密钥」一节；Compose 的主密钥接线已完成。**真实厂商的手工验收 2026-09-22 已跑**：产品负责人提供的 DeepSeek 密钥存进 `../.local-secrets/deepseek_api_key`（仓库外，未提交），实例管理员在设置页配好后「试一下」真发一次请求（179ms，回「你好」）；再把一把无效密钥的同一家排到第一位，聊天照常成功、网关自动换到第二家（同一次请求的日志里先 `http_401` 随后 `ok`），真实回复落在本子上。顺带修掉网关告警只打 `[object Object]` 的问题（见 CHANGELOG 同日条目）。同意说明里的厂商名与同意版本号升级仍待法务。

### 用户原话（2026-09-22）

> 在模型配置上面，把自定义模型配置弄进去，不能指望某个厂商永远给我提供服务。

### 现在的实现

- **网关**：`packages/llm-gateway/src/index.js` 已经支持多个供应商。
  - 用环境变量配置：`GATEWAY_PROVIDERS`，以及每家的 `GATEWAY_<NAME>_BASE_URL/_MODEL/_API_KEY/_SCOPE/_SCENES/_PRIORITY`，还有按场景路由的 `GATEWAY_SCENE_<scene>`；
  - 协议是 OpenAI 兼容的 chat completions。
- **API**：`apps/api/src/services/llmService.js` 的 `buildGatewayEnv()` 写死了只用一个槽位 `qwen`。
  - `isCloudProviderConfigured()` 只看 `GATEWAY_QWEN_*`；
  - `getGateway()` 进程内只建一次，缓存在 `gatewayPromise`。
- **启动校验**：`apps/api/src/config/runtime.js` 校验 `GATEWAY_QWEN_*`，内测环境要求 https，key 走文件。
- **向量**：记忆向量单独配置，是 `MEMORY_EMBEDDING_*`，见 `.env.example`。
- **管理员**：已有管理员概念，`INSTANCE_ADMIN_PHONES` 加中间件 `apps/api/src/middleware/instanceAdmin.js`，但**目前没有任何路由在用**。
- **界面**：设置页的「聊天模型」卡片 `apps/web/src/components/chat/CloudModelSettings.jsx` 只是用户的云端同意开关和数据说明，没有任何模型配置。

### 开工前要先问产品负责人

建议用 AskUserQuestion 一次问清：

1. **谁来配**：
   - (a) 实例管理员在应用里配（推荐）。「不能指望某个厂商给**我**提供服务」是运营者的视角；
   - (b) 每个用户自带模型和 key；
   - (c) 两者都要。
2. **支持哪些协议**：
   - 先只做 OpenAI 兼容（推荐）。DeepSeek、通义 DashScope 兼容模式、Moonshot、智谱、硅基流动、火山方舟，以及自建的 vLLM / Ollama 都走这个；
   - 还是也要 Anthropic 原生等其他协议。
3. **多家供应商和自动切换**：配几家，按顺序排；前一家失败就换下一家？网关本来支持按优先级依次尝试。
4. **key 存在哪**：
   - (a) 数据库里加密存储（推荐），用一把放在只读文件里的主密钥，比如 `MODEL_CONFIG_KEY_FILE`，AES-256-GCM；
   - (b) 继续用服务器上的只读文件，界面只能选、不能填。
5. **向量模型**要不要一起可配。
6. **合规**：换成境外厂商就是数据出境；换厂商后，云端同意说明里的「已配置的聊天模型」要不要写出厂商名，同意版本号要不要升。**这一条要交给法务，不要替它决定。**

### 推荐方案（按 1(a)、2 只做 OpenAI 兼容、3 多家依次切换、4(a)）

1. **迁移** `model_providers`：
   - 字段：`id`、`name`（显示名）、`base_url`、`model`、`api_key_encrypted`、`scenes`（chat / explain / work）、`priority`、`enabled`、`created_at`、`updated_at`、`updated_by`；
   - 不存明文 key；
   - 迁移前按约定验证备份。
2. **服务** `modelProviderService.js`：
   - 增删改查，key 只写不读，接口只返回 `hasKey`；
   - 用 `MODEL_CONFIG_KEY_FILE` 加解密；
   - 校验：`base_url` 必须是 https（只有本机运行时允许 http://127.0.0.1）；不得内嵌凭据；拒绝指向内网的地址（防 SSRF），可以复用 `runtime.js` 里现有的 URL 校验。
3. **网关装配**（改 `llmService.js`）：
   - `buildGatewayEnv()` 改为：从数据库读启用的供应商，拼出 `GATEWAY_PROVIDERS` 与各家变量；
   - 没配任何一家时，退回现有的 `GATEWAY_QWEN_*` 环境变量，这样现有部署不受影响；
   - 配置一变就清掉 `gatewayPromise` 重建；
   - `isCloudProviderConfigured()` 同时看数据库和环境变量。
4. **路由** `/api/admin/model-providers`：
   - 挂 `instanceAdminMiddleware`；
   - `POST /:id/test` 发一个最小请求试通，这会真实花一点钱，界面上要写明；
   - 改动写审计日志（不含 key）。
5. **前端**：设置页新增一张只有管理员可见的卡片「模型供应商」，放在「聊天模型」卡片旁边：
   - 列表、新增、编辑、排序、启停、「试一下」；
   - key 输入框只写不显示；
   - 普通用户看不到这张卡。
6. **同意说明**：`CloudModelSettings.jsx` 里写明当前在用的是哪家（显示名）。要不要升同意版本，交法务。
7. **文档**：
   - `API文档.md`、`.env.example`（`MODEL_CONFIG_KEY_FILE`）、部署手册（主密钥怎么生成、放哪、怎么备份）、CHANGELOG；
   - 路线图新增一条裁定：模型不绑定单一厂商。

### 怎么验

- **单测**：
  - 加解密往返，密文里看不出 key；
  - 接口不回显 key；
  - 非管理员 403；
  - https、内网地址、内嵌凭据三项校验；
  - 没有数据库配置时退回环境变量；
  - 配置变更后网关重建；
  - 多家时前一家失败切下一家，网关已有同类测试可参考。
- **e2e**：管理员能看到卡片并完成新增和启停，普通用户看不到；严格模拟里补上新接口。
- **手工**：接两家不同厂商，把第一家停掉，聊天自动走第二家。**接真实厂商会花钱，先问。**

---

## 三、之后的路线（已批准，未开工）

路线图 C9 的第三步「专为女生的那一层」。**开工前要产品负责人先定三件事（见下），本篇是细计划，没有开始写代码。**

### 0. 产品负责人已定（2026-09-22），四件事都按推荐裁定

| 问题 | 裁定 | 落地 |
|------|------|------|
| 8 月批准的女性心理提示词层按哪一份实现 | **点名把归档稿扶正** | 文档移到 `docs/01-产品/年轻女性心理与经典话题提示词设计_V1.0.md`（状态改 ✅ 当前有效），§3 的共享段接进 `packages/llm-gateway/src/personas.js` 的 `SHARED_PREAMBLE` |
| 开场话题的来路 | **三条都用**（惦记的事 + 在读的书 + 最近的手记） | `GET /api/chat/openers` + 空对话那一屏先静态池、拿到候选再替换；手记那条只作草稿 |
| 「懂你」检验集做到哪一层 | **先只做离线** | `apps/api/tests/knows-you/knows-you.cases.json` + `chatService.test.js` 里的runner；在线那一层（真模型 + 人工评分表）要花钱，未做 |
| 「身体关怀用已知的经期事实」要不要更进一步 | **现在这样就够** | 不需要新代码：C9 那条只调语气与分寸、不主动提、不归因、不进记忆 |

§1、§3、§4 已实施，§2 按上表不需要动代码（下面各节保留当初的做法说明，供回看）。

### 1. 女性心理提示词层接进共用前言（`packages/llm-gateway/src/personas.js`）

- 现状：六个人格共用 `SAFETY_PREAMBLE`，界面只暴露三种说话方式（温柔 / 直爽 / 安静，路线图 C7）。
- 做法：在被批准的稿子基础上，抽出一段**共用前言**（对所有说话方式都成立的部分：她怎么听、怎么不评判、不替用户下结论、不劝、不诊断），与安全前言的顺序写清楚（安全 > 共用前言 > 说话方式）。
- 验收：网关单测断言三种说话方式都带这段前言、安全前言仍在其上；`packages/llm-gateway` 现有 23 项不回归。

### 2. 身体关怀与经期事实（**不需要新代码，除非产品负责人要更多**）

- 路线图 C9 已经裁定：经单独同意后，周期**只调语气与分寸**、不主动提、不归因、不进记忆（第二步已实施并验证）。现有身体话题（`apps/web/src/features/openers.js` 的 `BODY`）沿用原措辞：只做科普、就诊准备与感受梳理。
- 所以这一条要请产品负责人确认：「用已知的经期事实」是指**现在这样就够了**（只在语气与分寸里用），还是要更进一步——更进一步会碰到 C9 那条「不主动提、不归因」的裁定，需要显式重新裁定。**不自行越线。**

### 3. 开场话题从她的线索里来（`apps/web/src/features/openers.js`＋一个只读接口）

- 现状：开场话题是本地静态池，按本机日期轮换（2 个随口 + 1 个身体 + 1 个情绪）。
- 建议做法：新增 `GET /api/chat/openers`（只读、只返回文案与来源标签），服务端按优先级挑最多 4 条：她惦记的、今天到日子的 → 在读的那本书 → 最近一条手记 → 补足静态池；每条标明「为什么看到这条」。前端空态先渲染静态池（不阻塞），拿到了再替换（避免空对话多三次请求）。
- 边界：**不发模型、不落库、不进记忆**；手记只取用户自己写的原文（若第 2 条点头）；删除线索立即不再出现；≤4 条、一天内同样的话题不重复。
- 验收：接口单测（挑取顺序、越界不取、删除后不出现、最多 4 条）＋ 前端单测（拿到前用静态池、拿到后替换、来源标签可读）＋ e2e 一条（有书/有惦记的事时开场出现对应话题，点一下照旧只填草稿或直接发）。

### 4. 一套冻结的「懂你」检验集（`apps/api/tests/knows-you/`）

- 冻结：一份版本化 fixture（场景 + 期望），改动要走产品负责人确认，不在实现里默默改期望。
- 每个场景断言**拼给模型的那段上下文**里有什么、没有什么，例如：称呼用她给你起的名字；放在心上那 5 条一定在；她知道现在几点、隔了多久；今天她主动说过的话在里面；**没同意周期时一个字都没有**；同意且正在经期时只有「在经期」这一句事实；中级危机走小心模式、高级整轮拦下；倾诉的一轮不塞工具目录。
- 验收：离线可跑、不花钱、进 CI；在线那层（真模型 + 人工评分表）默认不跑，产品负责人点头后再加。

### 5. 这一轮的收尾与下一步

- 四件都已落地（§1、§3、§4 实施，§2 确认不需要动代码），验证见 `CHANGELOG.md` 的「专为女生的那一层」。
- **没做、要另外问的**：心理顾问对共用前言与八类话题应对库的文案复核（文档里一直标着「建议补做」）；真模型的在线对照评审（要花钱）；「她惦记的事」在做梦关掉后要不要也在「她」页收起来（现在只收在对话与开场话题里，页面仍列出、可删——这是 2026-09-21 第一步定的口径，要改得产品负责人点头）。
- 再往后仍然是路线图 §8 的后置表（自动记忆提取与向量检索、天气、支付、语音/图片生成、真实短信、账号删除）。
